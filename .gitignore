# Scratch di lavoro del flusso subagent-driven (non versionare)
.superpowers/

# Segreti locali (password reali dei ruoli DB): mai nel repo. Versionare solo .env.example.
.env
models/

frontend/node_modules/
frontend/dist/
frontend/.env

# Playwright e2e output (browser binaries live in ~/.cache, outside the repo)
frontend/test-results/
frontend/playwright-report/
frontend/blob-report/
frontend/playwright/.cache/

operator-portal/node_modules/
operator-portal/dist/
operator-portal/.env

# Playwright e2e output (browser binaries live in ~/.cache, outside the repo)
operator-portal/test-results/
operator-portal/playwright-report/
operator-portal/blob-report/
operator-portal/playwright/.cache/

# run-stack.sh runtime (pids/logs)
.run-stack/
