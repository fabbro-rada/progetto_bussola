# STATO_TECNICO.md — Progetto «Bussola»

**Documento tecnico vivo** · Il «come» del progetto · *Ultimo aggiornamento: 2026-07-28*

---

## 0. Natura di questo documento

Questo è il **documento tecnico vivo** previsto da `CLAUDE.md` §12. Contiene il **come**: stack, modelli scelti e loro motivazione, architettura, comandi, modo di eseguire i test, decisioni operative.

- **Non è protetto.** Può evolvere liberamente man mano che il progetto avanza.
- **Non ridefinisce il cosa e il perché.** Missione, linee rosse, principi, modello del profilo, ruoli, funzionalità e criteri di successo vivono in `CLAUDE.md` (nucleo protetto, §1–§11). Se un'esigenza tecnica qui sembra contraddire il nucleo, **si applica la Regola del blocco** (§0 di `CLAUDE.md`): fermarsi, spiegare, chiedere conferma, aggiornare il nucleo solo dopo approvazione.
- **Flusso di sviluppo.** Ogni sottosistema segue: brainstorming → **spec di design** (`docs/superpowers/specs/`) → **piano di implementazione** (`docs/superpowers/plans/`) → **TDD**. La spec fissa *cosa* costruisce il sottosistema e *perché* (motivazioni delle scelte); il piano lo scompone in passi eseguibili con codice e comandi.

---

## 1. Hardware di riferimento (macchina del pilota)

Rilevato il 2026-07-20. Questa macchina (o una gemella) **è** l'hardware del pilota di Monza; il dimensionamento è pensato per starci dentro con margine.

| Componente | Dettaglio |
|---|---|
| Macchina | ASUS ROG Zephyrus G16 (GA605WI), portatile |
| OS | Ubuntu 24.04.4 LTS, kernel 6.8 |
| CPU | AMD Ryzen AI 9 HX 370 — 12 core / 24 thread, ~5.4 GHz |
| RAM | 30 GiB + 8 GiB swap |
| GPU | **NVIDIA RTX 4070 Mobile — 8 GB VRAM** (CUDA 13, driver 580) |
| iGPU | AMD Radeon 890M (integrata) |
| Disco | 972 GB (~572 GB liberi) |
| NPU | AMD XDNA 2 — **non utilizzata** (stack open su Linux ancora acerbo) |

**Il vincolo che comanda:** gli **8 GB di VRAM**. Con una sola postazione simultanea la regola d'oro è: **GPU solo per l'LLM, voce (STT/TTS) su CPU**. Nessuna contesa di VRAM, e la voce può degradare a testo senza mai bloccare il colloquio.

---

## 2. Architettura (single-box, `localhost`)

Kiosk della persona e portale operatore girano **sulla stessa macchina**, in sessioni separate. Nessun dato personale viaggia in rete: è la topologia più semplice e più sicura.

```
┌──────────────────────────────────────────────────────────────┐
│  POSTAZIONE UNICA (Ubuntu, solo 127.0.0.1)                    │
│                                                                │
│  Chromium --kiosk (persona)      Browser operatore (sessione  │
│         │                         separata, stesso box)       │
│         └──────────────┬───────────────────┘                  │
│                        ▼                                       │
│         Frontend web (React) — i18n 5 lingue, RTL arabo       │
│                        │  HTTP/WebSocket (solo 127.0.0.1)     │
│                        ▼                                       │
│              Backend applicativo (FastAPI)                    │
│    ┌───────────┬──────────────┬───────────────┬───────────┐  │
│    │ Guardrail │ Estrazione   │ Matching      │ Audit +   │  │
│    │ (ambito,  │ strutturata  │ spiegabile    │ ruoli     │  │
│    │  PII,inj) │ validata     │               │           │  │
│    └─────┬─────┴──────┬───────┴───────┬───────┴─────┬─────┘  │
│          ▼            ▼               ▼             ▼        │
│   llama.cpp       faster-whisper   Piper TTS    PostgreSQL   │
│   (LLM, GPU)      (STT, CPU)       (voce, CPU)  (segregato)  │
└──────────────────────────────────────────────────────────────┘
     GPU 8 GB ◄─ solo LLM        CPU 24 thread ◄─ tutta la voce
```

**Confine client-server pulito:** anche se oggi è tutto su un box, il backend espone un'API HTTP/WebSocket. Se in futuro il portale operatore dovesse spostarsi su una macchina in LAN, non serve riscrivere: si aggiungono solo TLS interno e segregazione di rete.

---

## 3. Stack tecnologico

| Livello | Scelta | Licenza | Dove gira |
|---|---|---|---|
| LLM | **Qwen2.5-7B-Instruct** (GGUF Q4_K_M) · fallback **1.5B** | Apache 2.0 | GPU |
| Serving LLM | **llama.cpp** (`llama-server`, API OpenAI-compatibile) | MIT | GPU |
| STT (riconoscimento) | **faster-whisper** (CTranslate2), `large-v3-turbo` int8 | MIT | CPU |
| TTS (sintesi) | **Piper** (`piper-tts` 1.x, OHF-Voice), voci it/en/fr/es | **GPL-3.0** ⚠️ (eccezione §3, vedi §4.3) | CPU |
| Filtro PII in uscita | **Presidio** (difesa in profondità) | MIT | CPU |
| Backend | **FastAPI** + Uvicorn (Python 3.12) | MIT/BSD | CPU |
| Estrazione validata | **Pydantic** + constrained decoding (GBNF/JSON-schema) | MIT | CPU/GPU |
| Frontend | **React + Vite + TypeScript**, `react-i18next` | MIT | Browser |
| Kiosk | **Chromium `--kiosk`** + utente Linux blindato | — | OS |
| Database | **PostgreSQL** (in Docker) | PostgreSQL Lic. | CPU |
| Test | **pytest** (backend), **Vitest** + **Playwright** (frontend/a11y) | MIT | — |
| Orchestrazione | **docker compose** | Apache 2.0 | — |

Tutte licenze aperte, gratuite e locali; nessun costo di licenza o servizio; nessuna API esterna per l'inferenza. Coerente con `CLAUDE.md` §3. **Unica eccezione alla clausola «permissiva»:** il motore TTS **Piper** è **copyleft (GPL-3.0)** — eccezione documentata e approvata dalla Direzione (2026-07-28), ammessa perché resta open/gratuito/locale e il pilota è on-premise non distribuito (obblighi copyleft non attivati). Dettaglio e motivazione in §4.3.

---

## 4. Modelli: scelte e motivazioni

### 4.1 LLM — Qwen2.5-7B-Instruct
- **Perché questo.** È tra i 7B multilingua più forti ed è **Apache 2.0** (permissivo, a differenza delle licenze «community» di Llama/Gemma). Eccelle su **arabo**, su **output strutturato/JSON** e sull'aderenza alle istruzioni: entrambe qualità decisive per guardrail affidabili ed estrazione conforme allo schema.
- **Quantizzazione.** GGUF **Q4_K_M** (~4.7 GB): miglior compromesso qualità/VRAM per gli 8 GB.
- **Fallback (degrado elegante).** **Qwen2.5-1.5B-Instruct** (~1.3 GB, **Apache-2.0**) se VRAM o latenza lo richiedono; in estrema economia **0.5B** (anch'esso Apache-2.0). **Correzione 2026-07-28 (§3):** il fallback era indicato come **3B**, ma `Qwen2.5-3B` ha licenza **`qwen-research`** (ricerca/non-commerciale, **non permissiva**) → sostituito con i tagli permissivi 1.5B/0.5B (nella famiglia Qwen2.5 sono Apache-2.0 i tagli 0.5/1.5/7/14/32B; 3B e 72B no). Il fallback è **solo documentale** (non cablato: il codice usa `BUSSOLA_LLM_MODEL`, default 7B), quindi nessun impatto sul runtime attuale.
- **Upgrade in produzione.** Stesso serving, si cambia solo il modello: **14B** (≥12 GB VRAM) o **32B** (≥24 GB). Nessuna riscrittura.

### 4.2 STT — faster-whisper
- Whisper copre tutte e 5 le lingue **incluso l'arabo**. `faster-whisper` (backend CTranslate2) in **int8** gira veloce sui 24 thread della CPU, lasciando la GPU all'LLM.
- Default `large-v3-turbo` (equilibrio qualità/velocità); configurabile a `large-v3` per la massima qualità sull'arabo o a `small`/`medium` per più velocità.

### 4.3 TTS — Piper
- Leggero, CPU-friendly, con voci per **it/en/fr/es** (arabo assente → testo, §8). Coerente con «prima il testo, la voce come potenziamento».
- **Licenza (correzione 2026-07-28): `piper-tts` 1.x è GPL-3.0, NON MIT.** L'assunzione originale «Piper MIT» valeva per il vecchio `rhasspy/piper` (0.x); il pacchetto PyPI attuale è la riscrittura **`OHF-Voice/piper1-gpl`**, **GPL-3.0-or-later** (la fonemizzazione lega espeak-ng, a sua volta GPL). Il codice lo importa **in-process** (`voice/tts.py`, `synthesize_wav`). **Eccezione a §3 approvata dalla Direzione:** GPL resta open/gratuito/locale; il pilota è **on-premise non distribuito** → gli obblighi copyleft (che scattano sulla distribuzione) non si attivano. **Da rivalutare** se si distribuisse/commercializzasse: allora isolare Piper come **processo separato** (arm's-length, la GPL lo consente) o sostituire il motore TTS con uno permissivo. Le **voci** restano permissive (paola/davefx CC0, ljspeech public-domain, siwis CC-BY-4.0) — vedi §14.
- **Arabo:** incluso come obiettivo; se la resa non fosse adeguata, resta garantito il **ripiego sul testo** (come da `CLAUDE.md` §8). Nessun blocco del colloquio in nessun caso.
- *Scartato:* XTTS-v2 (qualità superiore ma licenza del modello **non commerciale** → incompatibile con §3, e la NC è peggiore della GPL: né open né permissiva).

---

## 5. Dimensionamento VRAM e degrado elegante

| In VRAM | Costo |
|---|---|
| Qwen2.5-7B Q4_K_M (pesi) | ~4.7 GB |
| KV cache (~8k token contesto) | ~1.5 GB |
| **Totale LLM** | **~6.2 GB** → sta in 8 GB con margine |
| STT + TTS | 0 GB (su CPU) |

In produzione, spostando il desktop sull'**iGPU Radeon 890M** si libera quasi tutta la VRAM NVIDIA per l'LLM.

**Livelli di degrado elegante:**
1. **Voce → testo:** se STT/TTS sono lenti o assenti, il colloquio prosegue in testo, senza interruzioni.
2. **Modello grande → leggero:** 7B → 3B se serve.
3. **Arabo:** STT pieno; TTS come obiettivo con ripiego a testo.

---

## 6. Sicurezza e privacy — come realizziamo i vincoli

La sicurezza è la **priorità n.1**. Ogni garanzia del nucleo (`CLAUDE.md` §9) ha qui una realizzazione concreta.

- **Segregazione dei dati.** Schemi PostgreSQL separati (`profiles`, `audit`; futuro `conversations`) con **ruoli DB a privilegio minimo**: `bussola_owner` (DDL/migrazioni), `bussola_app` (RW profili, **solo-INSERT** audit), `bussola_auditor` (**solo-lettura** audit). Le distinzioni tra ruoli del §6 (operatore/supervisore/amministratore) sono **RBAC applicativo** (sottosistema «Auth & operatori»), non ruoli DB.
- **Pseudonimizzazione (minimizzazione massima).** Lo **pseudonimo è l'unico identificatore** dei profili. Il sistema **non memorizza alcun dato anagrafico né la mappa pseudonimo↔persona**: quel legame vive in un **registro esterno** dell'istituto. Niente tabella di mappatura, niente `pgcrypto` per l'identità (non c'è nulla di identità da cifrare).
- **Audit immutabile.** Tabella di audit **append-only**: `UPDATE`/`DELETE` negati al ruolo applicativo (default-deny) **e** vietati da un trigger a tutti tranne l'owner. In aggiunta, **hash-chaining** (ogni record include l'hash del precedente) per rendere evidente ogni manomissione. Cintura + bretelle. *Proprietà e limiti:* è **tamper-evident** (rileva modifiche/cancellazioni post-hoc), **non tamper-proof** contro un writer `app` compromesso che inserisca una catena falsa auto-consistente → HMAC con chiave / ancoraggio esterno = **Fase 2**. Il campo `details` dell'audit è un dict libero **non** filtrato dal PII e finisce in un log non-scrubbabile: va **vincolato/sanitizzato al confine** quando gli operatori guideranno gli eventi (sottosistema Auth & portale).
- **Cifratura a riposo e in transito.** A riposo: **LUKS full-disk** sulla macchina (passo di *deployment*). In transito: tutto su `127.0.0.1` (nessun dato in rete); se in futuro si va in LAN, TLS interno. (`pgcrypto` non serve: nessun dato di identità.)
- **Guardrail (in ingresso e in uscita):**
  - **Controllo dell'ambito:** il sistema risponde solo su lavoro/formazione/orientamento; ogni richiesta fuori tema è rifiutata con garbo, sia in input sia in output. Realizzato (Sott. 3) con un **guard layer indipendente**: guard di **input** = classificatore LLM strutturato (`{allow, category, reason}`, temp 0) + validazioni deterministiche; guard di **output** = ri-check di ambito via LLM **sempre attivo** (§2 «in uscita») + filtro PII + controlli anti-fuga. Rifiuti **strutturati** con messaggio localizzato non giudicante. Serving: **`llama-server` nativo** (CUDA), client httpx sull'endpoint OpenAI-compatibile.
  - **Resistenza a manipolazione (prompt injection) ed estrazione dati:** system prompt blindato, azioni consentite solo tra quelle previste, controlli indipendenti dal «buon comportamento» del modello.
  - **Filtro PII in uscita:** **Presidio** (MIT) come difesa in profondità sui testi liberi, prima di mostrare o salvare. Riconoscitori a **pattern** (email, telefono, IBAN, carta, IP) indipendenti dalla lingua e deterministici; **NER inglese** via `en_core_web_lg` (**MIT**). *Vincolo licenze (§3):* il modello NER italiano `it_core_news_lg` è **CC BY-NC-SA 3.0** (non-commerciale + copyleft), quindi **escluso**; l'italiano usa un tokenizer `spacy.blank("it")` (MIT) + i pattern. Il **NER multilingua** (nomi/luoghi per it/fr/es/ar) è un **follow-up** con un modello a licenza permissiva da verificare. La whitelist resta comunque la garanzia primaria.
  - **Garanzia strutturale primaria:** lo **schema-whitelist** del profilo (§7): per costruzione non può contenere reati, salute, dati familiari o punteggi sulla persona.
- **Postazione blindata (kiosk).** Chromium `--kiosk` sotto un utente Linux dedicato senza privilegi, scorciatoie disabilitate, nessuna navigazione libera.

---

## 7. Estrazione strutturata validata

Dalla conversazione si ricavano dati strutturati conformi a uno **schema definito**, scartando tutto ciò che non è ammesso.

- **Constrained decoding.** `llama.cpp` vincola l'output del modello a una grammatica **GBNF / JSON-schema**: il modello *non può* produrre campi fuori schema.
- **Validazione applicativa.** **Pydantic** ri-valida lato backend (doppio controllo).
- **Whitelist per costruzione.** Lo schema ammette **solo** dati lavorativi (competenze, esperienze, aspirazioni, formazione, lingue, note operative per categorie predefinite). Reati, posizione giuridica, salute, dati familiari, inferenze/valutazioni sulla persona **non sono rappresentabili**.
- **Realismo confermato dalla persona.** Il sistema riepiloga a fine sezione e a fine colloquio, chiede conferma/correzione, e sulle incongruenze pone domande gentili non giudicanti. **Chi conferma è la persona**, non l'operatore.

---

## 8. Multilingua e accessibilità

- **Lingue:** italiano, inglese, francese, spagnolo, arabo.
- **i18n:** stringhe rivolte all'utente **esternalizzate** (cataloghi), pronte per la traduzione (`CLAUDE.md` §11). Frontend con `react-i18next`; backend con cataloghi di messaggi.
- **RTL:** supporto destra-sinistra per l'arabo.
- **Accessibilità:** font grandi, alto contrasto, testo semplificato, lettura vocale, comando immediato per fermare la sessione. Verificata con test automatici (Playwright/axe).

---

## 9. Strategia di test (TDD)

Regole: **test prima del codice** (RED → GREEN → REFACTOR); **solo dati sintetici**, mai dati reali; personas sintetiche varie per età, lingua, competenze, esperienze, vicinanza al fine pena.

**Ordine di priorità (dal più importante):**
1. **Tenuta dei guardrail** — il controllo dell'ambito regge; i rifiuti sono controllati.
2. **Resistenza avversaria** — prompt injection ed estrazione dati.
3. **Nessuna fuoriuscita di dati personali** — filtro in uscita.
4. **Validatori dello schema** del profilo e **coerenza dell'estrazione**.
5. **Gestione delle incongruenze** e correttezza di **riepilogo & conferma**.

Strumenti: `pytest` (backend, guardrail e AI), `Vitest` + `Playwright` (frontend, e2e, accessibilità).

---

## 10. Struttura del repository (prevista)

> Si concretizza con il piano di implementazione; qui la forma d'insieme.

```
progetto_bussola/
├── CLAUDE.md                    # nucleo protetto (cosa/perché)
├── STATO_TECNICO.md             # questo documento (come)
├── docs/superpowers/
│   ├── specs/                   # spec di design, una per sottosistema
│   └── plans/                   # piani di implementazione (TDD), uno per sottosistema
├── backend/                     # package `bussola`: profilo, guardrail, estrazione, matching, dati
│   ├── src/bussola/
│   └── tests/                   # test (guardrail e sicurezza per primi)
├── frontend/                    # React — kiosk della persona (S9 testo, S10 voce)
├── operator-portal/             # React — portale operatore (S11+, app SEPARATA, autenticata)
├── models/                      # modelli GGUF/whisper/piper (non versionati)
└── docker-compose.yml           # PostgreSQL + servizi
```

---

## 11. Comandi

> **Sezione viva:** cresce con l'implementazione. Stato attuale = backend del Sottosistema 1.

**Setup del backend** (da `backend/`):

```bash
python3.12 -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
python -m spacy download en_core_web_lg   # NER inglese, MIT — unico modello NER scaricato
```

> **Nota licenze/riproducibilità (§3, §10):** si scarica **solo** `en_core_web_lg` (MIT). Il modello italiano `it_core_news_lg` è **CC BY-NC-SA** e **non** va installato/usato: l'italiano è coperto dai riconoscitori a pattern + tokenizer `spacy.blank("it")`.

**Esecuzione (gate di qualità)** (da `backend/`, con `.venv` attivo):

```bash
pytest -q          # test (guardrail e sicurezza per primi)
ruff check .       # lint
mypy src           # type-check (strict)
```

**Serving LLM (llama-server, OpenAI-compatibile).**

Prerequisito: un binario `llama-server` con accelerazione GPU sul `PATH`. Due vie:
- **Vulkan (validato, consigliato):** release prebuilt `bin-ubuntu-vulkan` di llama.cpp — usa la GPU NVIDIA via il driver, **nessun CUDA toolkit, nessuna build da sorgente**. Requisiti già presenti qui: `libvulkan.so.1` + ICD NVIDIA (`nvidia_icd.json`, dal driver 580).
- **CUDA:** build da sorgente con `-DGGML_CUDA=ON` (richiede CUDA toolkit/`nvcc`) — potenzialmente più veloce; **non** esiste un prebuilt CUDA per Linux.

**Validazione serving (2026-07-21, backend Vulkan, RTX 4070 Mobile 8 GB):** Qwen2.5-7B Q4_K_M usa **~4.76 GB di VRAM** (entra negli 8 GB con margine), latenza a caldo **~38 token/s**, suite avversaria dei guardrail **7/7** su GPU. Vulkan è quindi un'opzione di deployment semplice e sufficiente per il kiosk.

```bash
bash scripts/serve-llm.sh   # scarica il modello (una tantum, ~4.7 GB) e avvia il server su :8080
```

Lo script scarica i due shard del Q4_K_M ufficiale di Qwen2.5-7B-Instruct-GGUF in `models/`
(se non già presenti) e avvia `llama-server` puntato sul primo shard, con offload GPU
(`-ngl 999`) e contesto 8192. `models/` è in `.gitignore`: i pesi non vengono mai versionati.

**Setup e gate del frontend** (kiosk della persona — Sottosistema 9, da `frontend/`):

```bash
npm install                  # dipendenze (React 18, Vite 5, react-i18next; runtime tutte MIT)
cp .env.example .env         # impostare VITE_KIOSK_TOKEN sulla postazione blindata (mai versionato)
npm run dev                  # dev server; proxy di /kiosk → 127.0.0.1:8000 (backend S8)
```

**Gate di qualità** (da `frontend/`):

```bash
npm test          # Vitest + @testing-library/react (jsdom) — il gate di ogni task
npm run typecheck # tsc -b (strict)
npm run lint      # ESLint 9 flat config + typescript-eslint
npm run build     # tsc -b && vite build
```

> Playwright (e2e/accessibilità) è previsto ma **rimandato**: audit opzionale/manuale vs API S8 reale (§14). Il token del kiosk è iniettato a build (`VITE_KIOSK_TOKEN`); vive solo sulla postazione blindata.

**Setup e gate del portale operatore** (app separata autenticata — Sottosistema 11, da `operator-portal/`):

```bash
npm install                  # dipendenze (React 18, Vite 5, react-router-dom, react-i18next; runtime tutte MIT)
npm run dev                  # dev server su :5174 (accanto al kiosk :5173); proxy /auth,/operators,/job-requests,/profiles → 127.0.0.1:8000
npm test && npm run typecheck && npm run lint && npm run build   # gate di ogni task
```

> Il portale è **autenticato** (login → token Bearer in `sessionStorage`), a differenza del kiosk (token-dispositivo, anonimo). In produzione va su LAN con TLS interno (§12).

---

## 12. Percorso di upgrade in produzione

Lo stack non cambia; cambiano solo le taglie:
- **LLM:** 7B → 14B (≥12 GB VRAM) → 32B (≥24 GB).
- **Concorrenza:** oltre la postazione singola, valutare un serving a più alto throughput (es. vLLM) mantenendo l'API OpenAI-compatibile.
- **Rete:** portale operatore in LAN con TLS interno e segregazione di rete (confine client-server già pronto).

---

## 13. Registro delle decisioni

| Data | Decisione | Motivo |
|---|---|---|
| 2026-07-20 | Hardware del pilota = questa macchina (8 GB VRAM) | Confermato dall'utente; dimensiono per 8 GB |
| 2026-07-20 | Topologia single-box, solo `localhost` | Confermato; più semplice e più sicuro |
| 2026-07-20 | LLM = Qwen2.5-7B-Instruct (Apache 2.0), serving llama.cpp | Migliore 7B multilingua/arabo e strutturato, licenza permissiva, controllo VRAM |
| 2026-07-20 | Voce su CPU: faster-whisper (STT) + Piper (TTS) | Nessuna contesa di VRAM; arabo con ripiego a testo |
| 2026-07-20 | Database = PostgreSQL (Docker) | Segregazione a ruoli e audit append-only imposti dal DB |
| 2026-07-20 | Frontend = React + Vite + TS | i18n/RTL maturi, accessibilità, ecosistema |
| 2026-07-20 | Flusso: spec di design **per sottosistema** prima di ogni piano | Design rivedibile a granularità di sottosistema, separato dai passi eseguibili |
| 2026-07-20 | Filtro PII: rimosso `it_core_news_lg` (CC BY-NC-SA); IT a pattern + tokenizer blank, NER EN via `en_core_web_lg` (MIT); NER multilingua permissivo = follow-up | §3: solo licenze permissive; §10: donabile e replicabile |
| 2026-07-21 | Sott. 2: nessun dato identità nel sistema (mappa pseudonimo↔persona esterna); psycopg3 + migrazioni SQL; ruoli DB coarse (owner/app/auditor) + RBAC app; profilo JSONB; sanitize al salvataggio | Minimizzazione (§4/§5); trasparenza del DDL di sicurezza (§9); filtro in uscita (§7.3) |
| 2026-07-21 | Inserito sottosistema «Auth & operatori» (account, autenticazione, RBAC) prima del portale operatore | Operatore = principal autenticato; persona detenuta = solo pseudonimo, senza account |
| 2026-07-21 | Driver DB = `psycopg3` (LGPL-3.0), accettato come interpretazione larga di §3 | LGPL a link dinamico: nessun obbligo sul nostro codice, donation/commercial-friendly. Restano ESCLUSI copyleft forte (GPL) e non-commerciale (es. CC BY-NC). |

---

## 14. Debito tecnico e follow-up noti

Registrati dalle revisioni (nessuno bloccante; da affrontare al momento giusto):

- **Audit `details` → vincolare/sanitizzare** al confine degli eventi operatore (sottosistema Auth & portale): oggi è un dict libero non filtrato in un log immutabile.
- **Hash-chain → HMAC con chiave / ancoraggio esterno** (Fase 2) per resistere a un writer compromesso (oggi tamper-evident, non tamper-proof).
- **Transazione unità-di-lavoro** per il portale: oggi `ProfileRepository` e `append_audit` committano separatamente; per «nessuna azione senza il suo record di audit» servirà una transazione condivisa.
- **`ruff format --check` nel gate** + riformattare 2 file pre-esistenti del Sott. 1 (`pii.py`, `test_sanitize_profile.py`) in un commit di housekeeping.
- **`db-init/00-roles.sh`**: le password sono interpolate come literal SQL (init una-tantum, input del deployer) → avvertenza «niente apici singoli» o binding.
- **Test aggiuntivi (hardening):** idempotenza del runner di migrazioni (`[]` alla 2ª esecuzione); percorso fail-closed di `ProfileRepository.save` end-to-end; `details` con payload non banale per la hash-chain (normalizzazione numeri JSON).
- **`_CHAIN_LOCK_KEY`**: documentare uno schema di namespacing per gli advisory-lock (sono cluster-wide).
- **Sott. 4 — `output ScopeGuard` sui testi generati** (`check_output`, una chiamata LLM): non ancora applicato ai riepiloghi/chiarimenti (il filtro PII sì). Rischio basso (dati solo-lavoro, stessa sessione/persona); da chiudere quando S7 renderizza le stringhe.
- **Sott. 4 — grado di evidenza ottimistico:** col modello reale una competenza solo *dichiarata* può essere estratta come `evidence="certified"`; candidato a tuning del prompt d'estrazione (non un difetto strutturale).
- **Sott. 4 — copertura test minori:** wiring dell'`audit` hook non esercitato dai test (kwargs allineati a S2 ma non verificati end-to-end); ramo di rifiuto nella clarification finale non testato a livello unit; docstring `extraction.py` («unparseable/invalid») da precisare a «invalid (schema)».
- **Sott. 4 — `submit()` dopo `completed`** restituisce «unavailable» (messaggio fuorviante): valutare uno Step terminale dedicato.
- **Sott. 4 — `_final_summary`**: stringhe i18n inline nell'orchestratore; valutare di centralizzarle nel modulo messaggi (coerenza §11).
- **Sott. 5 — `must_change_password` non forzato come gate**: un operatore che deve cambiare password può chiamare altri endpoint prima di farlo → aggiungere il gate nel **sottosistema portale** (ora impatto basso: nessun endpoint di business esiste ancora, la temp password è ad alta entropia).
- **Sott. 5 — stringhe d'errore API i18n**: i messaggi in `api/errors.py` sono in italiano inline; esternalizzare quando arriva il frontend (S7).
- **Sott. 5 — `create_operator` rollback difensivo**: su `UniqueViolation` la transazione resta aborted; oggi innocuo perché `get_conn` chiude la connessione per-richiesta, ma introdurre un rollback al confine API prima di adottare un connection-pool.
- **Sott. 5 — `role text` senza CHECK a DB**; **catch `UniqueViolation` generico**; **`_fail` non `NoReturn`**; **`action: str` non `Literal`**; **`bootstrap.main()` senza test** + TOCTOU check-then-act su bootstrap concorrente (script run-once): hardening minori di S5 da valutare.
- **Sott. 5 — convenzione migrazioni**: `0004_auth.sql` crea schema+tabelle nello stesso file (0001 centralizzava gli schema). Convenzione (schema-per-file vs schema-upfront) da fissare qui.
- **Sott. 5 — test aggiuntivi**: idle-timeout di sessione in isolamento; asserzione del code-path `dummy_verify` (oltre a status/messaggio); fault-injection sul rollback atomico stato+audit.
- **Sott. 6 — grounding dell'evidenza enforced solo via prompt + test d'integrazione**: un check codice (substring dell'evidenza nel profilo) rifiuterebbe evidenze legittimamente parafrasate/tradotte (danneggia il recall) → non adottato; valutare in Fase 2 un ancoraggio più sofisticato (es. verifica per-skill).
- **Sott. 6 — follow-on del portale**: **metriche minime** e **export di base con autorizzazione** (§7.2/§7.3), **persistenza/storicizzazione degli esiti di matching** (Fase 2), **peso per grado di evidenza** nello scoring (ora frazione soddisfatta).
- **Sott. 6 — degrado**: `LlmUnavailable → 503` gestito; un errore LLM non-transport (es. HTTP 5xx del server → `HTTPStatusError`) resta 500 (comportamento S3 pre-esistente) — valutare una mappatura dedicata.
- **Sott. 6 — test minori**: ordine di ranking (con survivor a punteggi diversi), branch `search(language=)`/filtri combinati, wording duplicato dei motivi quando entrambi i segnali part-time scattano.
- **Sott. 7 — licenze delle voci Piper (§3): VERIFICATE il 2026-07-28 dalle MODEL_CARD ufficiali del repo `rhasspy/piper-voices` (HF).** Esito per voce (licenza del **dataset di training**, che la MODEL_CARD indica come licenza della voce):
  - **es_ES-davefx-medium** → dataset `davefx` (OHF-Voice), **CC0** → permissiva ✅.
  - **it_IT-paola-medium** → dataset `paolapersico1/Voice-Dataset-Italian`, `license: cc0-1.0` (**CC0**) → permissiva ✅.
  - **fr_FR-siwis-medium** → dataset SIWIS (CSTR Edimburgo), **CC-BY 4.0** → permissiva ✅ **con obbligo di attribuzione** (citare la SIWIS French Speech Synthesis Database nella doc/UI del pilota).
  - **en_US-lessac-medium** → dataset Blizzard 2013 Lessac, **«RESEARCH LICENCE AGREEMENT»** (copyright Voice Factory/Lessac, licenza per-persona con approvazione manuale, non redistribuibile, uso di ricerca) → **NON permissiva ❌ era BLOCCANTE.** ✅ **RISOLTO (2026-07-28, Sott. 23):** sostituita con **`en_US-ljspeech-medium` (LJ Speech, public domain, «trained from scratch» → nessuna derivazione da lessac)** in `voice/config.py` (default `BUSSOLA_PIPER_VOICE_EN`) + `fetch-voice-models.sh` + test di regressione §3 (`test_english_voice_is_permissive_ljspeech_not_lessac`, blocca il ritorno di lessac). (Alternative escluse **dalle rispettive MODEL_CARD**: `en_US-ryan` è CC-BY-**NC**-SA → NC non ammesso; `en_US-arctic`/`libritts` sono comunque finetuned da lessac.)
- **LLM di fallback 3B → 1.5B (§3, correzione doc-only 2026-07-28).** Il fallback documentato `Qwen2.5-3B-Instruct` ha licenza `qwen-research` (non permissiva); sostituito nella doc (§3 tabella, §4.1) con `Qwen2.5-1.5B/0.5B-Instruct` (Apache-2.0). **Non era cablato** (il codice usa solo il 7B Apache-2.0 via `BUSSOLA_LLM_MODEL`), quindi nessun impatto runtime. Se un giorno si wireà un fallback, usare un taglio permissivo (0.5/1.5/7/14/32B), mai 3B/72B.
- **Motore TTS Piper GPL-3.0 — eccezione §3 accettata; RIVALUTARE alla distribuzione.** `piper-tts` 1.x (OHF-Voice) è GPL-3.0 (non MIT come assunto in S7), importato in-process. Eccezione approvata (on-premise non distribuito → copyleft non attivato; `CLAUDE.md` §3 + §4.3 + riga di decisione 2026-07-28). **Trigger di rivalutazione:** se Bussola venisse distribuito a terzi o commercializzato, gli obblighi GPL si attivano → isolare Piper come **processo separato** (arm's-length: refactor `voice/tts.py` da import a subprocess/CLI, la GPL lo consente) **oppure** sostituire il motore TTS con uno permissivo (con fonemizzatore permissivo, non espeak-ng). Fino ad allora nessuna azione.
- **Sott. 23 — attribuzione CC-BY-4.0 della voce francese (siwis): DA IMPLEMENTARE prima del pilota (§3).** `fr_FR-siwis` è CC-BY 4.0 → la licenza **richiede l'attribuzione**. Aggiungere un credito visibile — nella doc di deployment e/o in una schermata «info/crediti» del kiosk — che citi la **SIWIS French Speech Synthesis Database** (CSTR, Università di Edimburgo). Oggi **assente** in `frontend/`/`operator-portal/`. Bloccante di *conformità* pre-pilota (non di codice), come la revisione arabo-madrelingua.
- **Sott. 7 — caveat trasversale «finetuning da lessac» (§3): rischio residuo ACCETTATO e documentato (decisione utente/Direzione, 2026-07-28).** Le voci it/es/fr (e la maggior parte delle voci Piper) sono **«Finetuned from U.S. English lessac voice»**, i cui pesi derivano dal dataset Blizzard research-only. I loro **dataset** sono permissivi (CC0/CC0/CC-BY); se i pesi finetuned ereditassero la restrizione della base, questa si propagherebbe — questione legalmente **non risolta**. **Decisione: accettare il rischio residuo**, perché: pilota **non commerciale** di pubblico interesse; dataset delle voci permissivi; consenso pratico della comunità TTS (i pesi sono trattati come **non-derivati** dall'audio di training); `rhasspy` ridistribuisce liberamente sotto le licenze per-voce. L'inglese (lessac) era l'**unico** blocco netto ed è stato comunque rimosso (sopra). *Rivalutare se il progetto diventasse commerciale o se emergesse giurisprudenza contraria.*
- **Sott. 7 — requisito per S8 (§3, metà «voce lenta → testo»): timeout/asincronia.** I servizi STT/TTS sono **sincroni e senza timeout**: il contratto copre il *fallimento* (`VoiceUnavailable`/`None`) ma non la *latenza*. S8 (frontend/kiosk) DEVE avvolgere `transcribe`/`synthesize` con un timeout o chiamata non-bloccante (mostra subito l'input di testo alla scadenza), altrimenti una trascrizione lenta su CPU blocca il colloquio. Criterio d'accettazione esplicito di S8.
- **Sott. 7 — arabo-TTS**: attivabile aggiungendo una voce araba **permissiva e adeguata** a `PIPER_VOICES` dopo validazione sul pilota (una riga, nessuna modifica al codice).
- **Sott. 7 — validazione voce sul pilota**: misurare qualità/latenza reali su STT (dialetti arabi inclusi) e TTS; eventualmente salire di modello Whisper o attivare `cuda` se la VRAM lo consente.
- **Sott. 8 — hardening input (§3 prevenzione abusi):** l'endpoint `/kiosk/voice/transcribe` bufferizza l'audio caricato senza tetto di dimensione (`await audio.read()`); e `SynthesizeRequest.text` è senza `max_length`. Rischio basso (token+localhost+un utente; sul path persona il testo è già limitato dal colloquio e il TTS lento va in timeout), ma aggiungere un cap di dimensione/lunghezza come difesa-in-profondità.
- **Sott. 8 — registry di sessione non thread-safe / single-process:** `InterviewRegistry` (in-memory, `_sweep`/`pop` non lock-ati) assume **una sessione concorrente** (una postazione). Multi-worker/multi-postazione o thread concorrenti = **Fase 2** (store condiviso + lock).
- **Sott. 8 — endpoint di abort esplicito (§4):** oggi la volontarietà è soddisfatta (abbandono → TTL-sweep chiude la conn; sezioni confermate già persistite), ma il comando «ferma» è una preoccupazione della UI kiosk (S9). Valutare un endpoint `/kiosk/interview/end` esplicito.
- **Sott. 8 — DB down a `start` → 500:** onesto (niente persistenza = niente colloquio), non uno scenario di degrado §3; la conn viene comunque chiusa sul fallimento. Nessuna azione, annotato.
- **Sott. 9 (UI kiosk, primo frontend) — stringhe arabe da rivedere da un madrelingua prima del pilota (§8):** i cataloghi `ar` sono una traduzione «al meglio», non validata; RTL già applicato. Bloccante di *qualità* pre-pilota, non di codice.
- **Sott. 9 — audit accessibilità/e2e rimandato:** aggiungere Playwright + axe contro l'API S8 reale su localhost (smoke e2e + audit a11y automatico); oggi coperto solo da unit/component test (Vitest + RTL).
- **Sott. 9 — «opzioni rapide» del §7.1 non native:** il motore colloquio (S4) produce domande a **testo libero**; la UI realizza «domande brevi / il sistema conduce» ma non opzioni strutturate a scelta. Evoluzione futura del motore colloquio (non modifica del nucleo; documentato nella spec S9).
- **Sott. 9 — guard doppio-invio solo via `disabled` DOM:** gli handler `submit`/`start` non hanno un `if (state.pending) return`; oggi sufficiente (il bottone è l'unico trigger, provato da test con promise deferita), ma aggiungere difesa-in-profondità a livello handler se si introduce un trigger da tastiera/`<form>`. Idem: nessun `finally` che azzeri `pending` se un client alternativo sollevasse (il client reale è fail-closed).
- **Sott. 9 — licenze transitive dev-only fuori dalla lista MIT/Apache/BSD:** `caniuse-lite` (CC-BY-4.0, dati) e `argparse` (Python-2.0), più alcune advisory `npm audit` in dev-deps transitive — **solo build-time, non nel bundle `dist/`** (i runtime dep sono tutti MIT). Valutare una allowlist `license-checker`.
- **Sott. 9 — hardening minori (da review):** `initialState` restituito per riferimento (usare `{...initialState}`) e nessuna exhaustiveness-guard sulle union `Action`/`Screen`; context `TextSize` non memoizzato; alcuni test brief-mandated non asseriscono tutto (clear del campo, Send-disabled-quando-vuoto, assenza bottone in Unauthorized); prop types inline ripetuti; hex hardcoded in `theme.css` fuori dai token. Nessuno impatta il comportamento o una linea rossa.
- **Sott. 10 (voce kiosk) — BLOCCANTE-qualità prima del pilota: round-trip WebM/Opus reale vs faster-whisper.** `MediaRecorder` produce WebM/Opus, mai testato end-to-end contro `/kiosk/voice/transcribe` (finora solo WAV). PyAV *dovrebbe* decodificarlo, ma va validato con una registrazione reale in Chromium; se fallisse, registrare WAV via WebAudio o decodificare a monte. Top-rischio della spec S10.
- **Sott. 10 — `new MediaRecorder(stream)` non in try/catch:** un throw del costruttore lascerebbe `busyRef` stuck true + stream non fermato (mic-hot). Probabilità bassissima (getUserMedia già riuscito), pre-esistente al fix; aggiungere try/catch che ferma lo stream + reset busy + stato `unavailable`. Ultima voce «mic-hot» rimasta.
- **Sott. 10 — voce TTS araba assente:** S7 non ha voce `ar` permissiva → `synthesize(ar)`=204 → niente lettura in arabo (resta il testo, §8); la **dettatura** araba (Whisper) funziona. Aggiungere una voce `ar` permissiva a `PIPER_VOICES` = follow-up backend.
- **Sott. 10 — minori (da review):** scaffolding fetch duplicato in `voiceClient`; assert token `toBeDefined`; `stubMedia` sostituisce l'intero `navigator` (test); ramo catch-denied di `useRecorder` senza test dedicato; label Parla/Stop dal testo visibile (niente `aria-label` dedicata come mute/Ascolta); coverage sottile su alcuni stati. Nessuno impatta il comportamento o una linea rossa.
- **Sott. 11 (portale operatore, sotto-progetto 1/5) — interceptor globale 401→logout+avviso per i sotto-progetti feature.** Oggi il degrado 401 è gestito su bootstrap `me()` e su `changePassword` (→ logout + «sessione scaduta»); quando arriveranno le chiamate feature (matching/profili/…) serve un wrapper condiviso che, su 401, chiami `onUnauthorized()` e navighi a `/login` con l'avviso. Gli agganci (`onUnauthorized`, flag `sessionExpired`) sono già pronti.
- **Sott. 11 — UX password-vecchia-errata:** `changePassword` mostra un errore generico su qualunque non-204/401; distinguere «password attuale errata» quando il backend lo segnala.
- **Sott. 11 — roadmap sotto-progetti 2–5 del portale:** (2) flusso matching (richieste di lavoro + matching spiegabile), (3) consultazione profili, (4) amministrazione utenze, (5) metriche + export (con il **follow-on backend**: endpoint metriche + export con autorizzazione §7.3, oggi assenti).
- **Sott. 11 — minori (da review):** `AuthContext` value non memoizzato; `useAuth`-throw-fuori-provider e `allowMustChange=true` non testati a livello unit (coperti end-to-end); advisory `npm audit` dev-only (stessa toolchain del kiosk), nessun `engines`/`.nvmrc`. Nessuno impatta il comportamento o una linea rossa.
- **Sott. 12 (portale — richieste + matching) — roadmap sotto-progetti 3–5:** (3) consultazione profili (`GET /profiles`), (4) amministrazione utenze (endpoint `operators` S5), (5) metriche + export con il **follow-on backend** (endpoint metriche + export con autorizzazione §7.3, oggi assenti).
- **Sott. 12 — interceptor 401 globale realizzato per le chiamate feature:** `useApiError` (401→onUnauthorized+/login) è usato per list/get/create/match; chiude il follow-up S11 per questa superficie. Estendere lo stesso pattern alle sezioni future.
- **Sott. 12 — minori (da review):** coerenza del pattern unmount-guard tra le 3 schermate (`active` vs `mountedRef`); ramo «incompatibile» di `MatchResults` è difensivo-only (S6 restituisce solo compatibili — aggiungere un commento); DRY dello skeleton client + cast senza validazione runtime (stile pre-esistente); righe lingua del form usano `aria-label` non `<label>` e i branch add/remove-lingua + errore-creazione non hanno test dedicati. Nessuno impatta il comportamento o una linea rossa.
- **Sott. 13 (portale — consultazione profili) — roadmap sotto-progetti 4–5:** (4) amministrazione utenze (endpoint `operators` S5 già esistenti), (5) metriche + export con il **follow-on backend** (endpoint metriche + export con autorizzazione §7.3, oggi assenti). Con S13 il ciclo di consultazione (ricerca/filtri/dettaglio) del portale è completo.
- **Sott. 13 — rotte solo auth-gated (no role-gate client-side):** `/profiles` e `/profiles/:pseudonym` sono dietro `ProtectedRoute` ma non hanno un gate di ruolo lato client; il **server impone l'RBAC** (403→«non hai i permessi») e la nav nasconde le sezioni fuori-ruolo. Coerente con «il server resta l'autorità»; un role-gate client-side sarebbe solo UX. Pre-esistente al pattern S11/S12.
- **Sott. 13 — minori (da review):** empty-state incoerente nel dettaglio (formazione/note omesse quando vuote vs aspirazione mostra «—»); DRY del boilerplate `operatorClient` (stile pre-esistente); il fake `getProfile` ignora l'arg `pseudonym` (plan-mandated, restituisce sempre la fixture). Nessuno impatta il comportamento o una linea rossa.
- **Sott. 14 (portale — amministrazione utenze) — sotto-progetto 5 (ultimo) + follow-on backend:** resta (5) metriche + export con autorizzazione (§7.2/§7.3) — gli endpoint backend (metriche, export autorizzato) **non esistono ancora** → richiede un sottosistema backend prima del frontend. Con S14 le sezioni operatore del portale (richieste/matching, profili, utenze) sono complete; mancano supervisore (metriche/attività), auditor (log), admin-config.
- **Sott. 14 — follow-on backend `operators`:** nessun endpoint di **modifica** (rinomina/cambio ruolo) né di **cancellazione** (per §7.3 si disabilita, non si cancella). Correzione di un ruolo errato = disabilita + ricrea. Se serve la correzione in-place, aggiungere un endpoint di update (+audit) — decisione di prodotto rimandata.
- **Sott. 14 — copy della temp-password su contesto non-sicuro:** `navigator.clipboard` è disponibile su `localhost` (contesto sicuro) → sul pilota single-box funziona; su LAN in chiaro (non-localhost HTTP) sarebbe assente. Il fix rende «Copiato» affidabile (appare solo su copia riuscita; altrimenti la password resta a schermo da leggere). In produzione LAN si va comunque in TLS (§12), che ripristina la clipboard.
- **Sott. 14 — modali senza focus-trap/Escape:** `TempPasswordModal`/`ConfirmDialog` hanno `role="dialog" aria-modal` ma niente focus iniziale/trap né Escape-per-annullare. Gap di ergonomia tastiera/a11y del **portale** (non la superficie kiosk §4); accettabile per il pilota, da rifinire.
- **Sott. 14 — minori (da review):** `isSelf = me?.id === op.id` no-op se `me` fosse null (irraggiungibile dietro `ProtectedRoute`; il lockout è difesa-in-profondità, il server resta l'autorità); coverage gap (nessun test e2e sul path enable-success; rami JSON-invalido/5xx non testati per list/create/reset); cosmetici (`if (res.status === 204 || res.ok)` disjunct `204` morto; espressione di validità duplicata in `CreateOperatorForm`). Nessuno impatta il comportamento o una linea rossa.
- **Sott. 15 (metriche, sotto-progetto 5a) — resta 5b Export con autorizzazione (§7.3):** ultimo pezzo del portale; workflow con stato (operatore richiede → approvatore approva → estrazione + audit), tabella nuova, follow-on backend. Gli esiti del matching **non sono persistiti** → l'export Fase 1 riguarda i profili; reportistica aggregata/serie storiche = Fase 2 (§8).
- **Sott. 15 — «attività operatori» (pannello supervisore) + admin-config + auditor (log):** sezioni ancora scoperte, tutte dipendenti da backend/endpoint da costruire. §6.
- **Sott. 15 — `compute_metrics` carica tutti i profili in Python** per la completezza (scelta di spec: letture aggregate, semplicità). Ok a scala pilota (una postazione); se la popolazione cresce, spostare il calcolo lato SQL (JSONB). Non bloccante.
- **Sott. 15 — minori (da review):** test 403 esercita solo `OPERATOR` (admin/auditor pure senza `VIEW_METRICS`); ramo 401→unauthorized di `getMetrics` non testato a unit (coperto e2e dal pannello); 5 card ripetitive (array-driven); «Caricamento…» senza `aria-live`/`role="status"` (portale, non kiosk §4). Nessuno impatta il comportamento o una linea rossa.
- **Sott. 16 (export backend, 5b) — 5b-frontend è il prossimo passo:** superfici operatore (richiesta con filtri + motivo, lista con stato, **download nel browser** del JSON via fetch autenticato→Blob) e supervisore (**coda approvazioni** pending + approva/nega). Consuma il contratto `/exports` di questa parte. Con esso il ciclo operatore del portale è concluso; restano scoperti (backend da costruire): attività-operatori (supervisore), auditor (log), admin-config.
- **Sott. 16 — `PiiRedactor()` per-richiesta (trasversale, non introdotto qui):** i path di lettura profili (S13 `/profiles`, e ora il download export) costruiscono un `PiiRedactor` per richiesta — carica `en_core_web_lg`+Presidio (documentato «build once»), ma `ProfileRepository.search` **non redige** (i profili sono già sanitizzati al save). Spreco su hardware modesto (§10). Follow-up **codebase-wide**: iniettare un redactor condiviso/cached (pattern già in `api/kiosk/deps.py`) o togliere il redactor dai metodi di sola lettura di `ProfileRepository`.
- **Sott. 16 — `filters={}` = export dell'intera popolazione:** valido e mitigato da approvazione+audit, ma l'approvatore vede solo `filters` vuoto. Nel **5b-frontend** rendere esplicito «tutti i profili» all'approvatore; e registrare nell'audit del download qualcosa di più chiaro di `"filters": ""` per un export di massa.
- **Sott. 16 — minori (da review, non bloccanti):** `ExportService.create_request` non ri-valida `reason` a livello service (il router impone 1..500; il service è chiamato solo dai test → difesa-in-profondità); nessuna paginazione/streaming sul download (materializza tutti i profili in memoria — ok a scala pilota, rivedere per scala/Fase 2); `GET /download` ha un side-effect di audit (retry/prefetch sovra-contano, direzione sicura); nessun indice su `status`/`requested_by` (trivial a scala pilota); import `Jsonb` locale, `assert row` post-INSERT, `_decide(status: str)`→`Literal`, messaggi eccezione terse. Nessuno impatta il comportamento o una linea rossa.
- **Sott. 17 (export frontend, 5b) — ciclo operatore del portale CONCLUSO.** Restano scoperti, tutti con backend/endpoint ancora da costruire: pannello **attività operatori** (supervisore, §6), **auditor** (log di audit in sola lettura, §6), **admin-config** (configurazione sistema). Sono i pezzi rimanenti verso il completamento del portale.
- **Sott. 17 — classe di footgun i18n (label nav == titolo pagina):** `nav.export`==`exports.title` («Export») e `nav.exportApprovals`==`exports.approvalsTitle` («Approvazioni export»). Quando la voce nav è `built`, la stessa stringa compare sia nel link sia nell'`<h1>` → le query di test per **testo puro** diventano ambigue; i test d'integrazione usano `findByRole('heading'|'link')` per disambiguare. Convenzione per le sezioni future: query per ruolo, o etichette nav/titolo distinte.
- **Sott. 17 — minori (da review, non bloccanti):** ramo 401 non testato a unit per 5/6 metodi export (boilerplate identico ai sibling); ramo re-fetch conflict/not-found di approve/deny non testato (corretto per costruzione); flash one-frame di errore vuoto su download-unauthorized (`setError('')` poi redirect); markup filtri di `NewExportForm` quasi-duplicato di `ProfileSearch` (dedup se arriva un terzo consumer); `exports.downloadError` «Riprova» mostrato anche su 404 (retry fuorviante per una richiesta inesistente). Nessuno impatta il comportamento o una linea rossa.
- **Sott. 18 (auditor backend) — auditor-frontend è il prossimo passo:** il visore del log (elenco paginato + filtri chi/cosa/quando + badge integrità da `/audit/verify`) sul contratto di questa parte. Poi restano: attività-operatori (supervisore, costruibile sopra lo stesso contratto audit) e admin-config.
- **Sott. 18 — read path via `bussola_app`, non `bussola_auditor` (decisione di spec §3.6, difesa-in-profondità):** gli endpoint `/audit` usano `get_conn` (ruolo DB `bussola_app`, che ha SELECT+INSERT). La sola-lettura poggia su (a) disciplina app-code (gli endpoint fanno solo SELECT), (b) trigger append-only (blocca UPDATE/DELETE, non INSERT). Il ruolo DB read-only `bussola_auditor` (grant SELECT-only, 0003) **esiste ma non è cablato** al read path (non c'è switching di connessione per-ruolo). **Hardening futuro:** instradare `/audit` su una connessione `bussola_auditor` quando si introdurranno connessioni per-ruolo → sola-lettura imposta a livello DB.
- **Sott. 18 — minori (da review, non bloccanti):** `limit`/`before` senza `Query(ge=1)` (clamp lato `list_audit` sicuro, ma non self-documenting nell'OpenAPI); `from`/`to` naive vs colonna `timestamptz` (semantica TZ non pinnata; edge); `/audit/verify` carica l'intera catena in memoria (`verify_audit_chain` O(n), pre-esistente S2, ok a scala prototipo → verifica incrementale/streaming in futuro); coverage gap (`to_ts`/combinazione filtri, `limit<=0`, cursore `before` al layer HTTP — coperto a livello dati). Nessuno impatta il comportamento o una linea rossa.
- **Sott. 19 (auditor frontend) — RUOLO AUDITOR CONCLUSO.** Con questo il ruolo Auditor (§6) è completo (backend S18 + frontend S19). Restano verso il completamento del portale: **attività operatori** (supervisore, costruibile sopra lo stesso contratto `/audit`) e **admin-config** (l'unico ancora da scopare in un brainstorming più ampio).
- **Sott. 19 — minori (da review, non bloccanti):** timestamp reso con `occurred_at.replace('T',' ').slice(0,16)` → **scarta il `Z`** (UTC leggibile come locale; per un audit «quando» conviene un indicatore TZ); rami 401/5xx/JSON-invalido di `listAudit`/`verifyAudit` non testati a unit (precedente `getMetrics`); fake `auditPages: []` indicizzerebbe `-1` (guard `?? {status:'error'}` futuro); contenuto gated solo da `entries===null` post-mount (un'azione fallita successiva mostra il banner sopra la tabella stantia — accettabile). Nessuno impatta il comportamento o una linea rossa.
- **Sott. 20 (attività operatori) — RUOLO SUPERVISORE CONCLUSO.** Il supervisore ora ha metriche (S15), approvazione export (S16/S17) e attività operatori (S20). Verso il completamento del portale resta **solo admin-config** (amministratore, §6 CONFIGURE_SYSTEM) — l'unico non ancora scopato (cosa è configurabile va deciso in un brainstorming più ampio).
- **Sott. 20 — invariante «classificazione per azione, non per ruolo» (documentata in `activity/service.py`):** «attività operatori» conta le 5 azioni di lavoro, che oggi sono **operator-exclusive per RBAC** (READ_PROFILES/RUN_MATCHING/EXPORT_DATA). Se un sottosistema futuro concedesse una di queste a un altro ruolo, quel ruolo comparirebbe qui senza modifiche a questo codice (resterebbe comunque attività di **staff**, non violazione §2). Commento + test di esclusione presenti; tenerlo a mente estendendo l'RBAC.
- **Sott. 20 — minore (da review):** timestamp `last_active` reso via string-slice (stesso pattern di S19 AuditLog) → candidato a un **formatter di date condiviso** (con indicatore TZ) come cleanup trasversale; nessun test unmount-before-resolve (gap trasversale ai pannelli, come MetricsPanel). Nessuno impatta il comportamento o una linea rossa.
- **Sott. 21 (admin-config) — PORTALE OPERATORE COMPLETO: tutti e 4 i ruoli §6 coperti** (operatore: richieste+matching/profili/export; supervisore: metriche/attività/approvazione-export; amministratore: utenze/configurazione; auditor: log). Fase 1 del portale conclusa.
- **Sott. 21 — chore di consolidamento (ora che i 4 pannelli sono tutti `built`):** ✅ **risolto in Sott. 22** — (a) commento `nav.ts` aggiornato (flag `built?` mantenuto, scelta approvata); (b) `SUPPORTED_LANGUAGES` deduplicato in `bussola/languages.py` (modulo neutro dependency-free); (c) hook `useFetchOnMount` condiviso tra i 3 pannelli; (d) `formatTimestamp` deduplicato (S19/S20 string-slice) **senza** indicatore TZ — l'indicatore TZ resta un miglioramento a parte (vedi Sott. 22).
- **Sott. 21 — env-docs (§7.3, non codice):** `llm_base_url` è mostrato verbatim nel pannello configurazione. Ok col default credential-free `127.0.0.1:8080` (llama-server non autenticato, §3), ma **non mettere credenziali** in `BUSSOLA_LLM_BASE_URL` (`http://user:pass@host`): il pannello le mostrerebbe a un admin. Annotare nella doc di deployment.
- **Sott. 21 — minori (da review):** test endpoint non stubbano il seam LLM (probe reale a 127.0.0.1:8080, connection-refused→False istantaneo, valore non asserito → innocuo; stub per test pienamente ermetici); `test_view_is_audited` non asserisce `target_pseudonym IS NULL` (codice corretto); ramo 401 di `getSystemConfig` non testato a unit (convenzione sibling). Nessuno impatta il comportamento o una linea rossa.
- **Sott. 22 (consolidamento) — follow-up trasversali rimandati (esplicitamente fuori scope, refactor a comportamento invariato):** (a) **`formatTimestamp` con indicatore TZ**: oggi output byte-identico allo string-slice (nessun `Z`); un formatter TZ-aware è un *miglioramento di resa* separato (cambierebbe l'output → non un dedup). (b) **`PiiRedactor` condiviso/cached codebase-wide** (già follow-up S16): scope trasversale, intervento dedicato. (c) **`ruff format --check` nel gate** + riformattazione dei file pre-esistenti: modifica del gate CI, separata. (d) **`useFetchOnMount` senza `.catch()`**: una promise *rifiutata* (vs risolta in `{status:'error'}`) diventa unhandled rejection — **non una regressione** (identico ai 3 pannelli originali), ma ora che vive in un punto solo è candidato a hardening centralizzato.

---

## 15. Registro delle decisioni (segue)

| Data | Decisione | Motivo |
|---|---|---|
| 2026-07-21 | Sott. 3: guard layer **indipendente** (guard input = classificatore LLM strutturato temp 0; guard output = ri-check ambito LLM **sempre attivo** + filtro PII) | §2 ambito in ingresso **e in uscita**; §7.3 controlli indipendenti dal buon comportamento del modello |
| 2026-07-21 | Sott. 3: serving `llama-server` **nativo** (CUDA), client **httpx** verso endpoint OpenAI-compatibile; modello Qwen2.5-7B GGUF Q4 (Apache 2.0) | Toolkit GPU per Docker assente → GPU nativa più semplice; dipendenze minime; nessuna API esterna |
| 2026-07-21 | Serving GPU **validato via Vulkan** (prebuilt, no CUDA toolkit): VRAM ~4.76 GB, ~38 tok/s, guardrail 7/7 su GPU. Vulkan = opzione di deployment del kiosk | Nessun prebuilt CUDA Linux; Vulkan usa la GPU col solo driver → più semplice/replicabile (§10) |
| 2026-07-21 | Sott. 4 (colloquio): flusso **deterministico** guidato dall'app; estrazione **per-sezione con constrained decoding**; incongruenze via **LLM semantico**; riepilogo & conferma **dalla persona**; stato in-memory + persistenza per-sezione | §7.1 «il sistema conduce»; §7.3 estrazione validata; §5 validazione con la persona; ripresa a metà sezione = Fase 2 |
| 2026-07-23 | Sott. 4 concluso + validato end-to-end con Qwen2.5-7B reale su GPU (Vulkan) + Postgres. Estrazione vincolata **eccellente**; validazione reale ha corretto 2 comportamenti del modello (vedi §14/remediation): incongruenze **a fine colloquio** (prompt rigoroso, mai campi mancanti) e **precisione dello scope-guard** (esempi in-ambito, no falso «lingua diversa»), guard avversario **7/7** riverificato | Il metro è «si comporta come promesso quando messo alla prova» (§10): il test live ha fatto emergere difetti che asserzioni deboli avrebbero nascosto |
| 2026-07-23 | Sott. 4: **filtro PII in uscita anche «prima di mostrare»** — `PiiRedactor` (Presidio, no LLM) applicato ai testi generati (riepilogo/chiarimento) nell'orchestratore, oltre al filtro già presente a persistenza | Nucleo §7.3 richiede il filtro «prima di mostrare **o** salvare»; rilevato dalla review finale opus |
| 2026-07-23 | Sott. 5 «Auth & operatori»: **autenticazione a password (argon2id) + sessioni server-side + RBAC + primo layer HTTP (FastAPI)**. Sessioni: token opaco, in DB solo l'hash SHA-256, scadenza assoluta+idle, revoca immediata. Account provisionati **solo dall'Amministratore** (no self-signup), bootstrap CLI senza credenziali di default. Login: errore generico + dummy-verify (no user-enumeration) + lockout. Ogni evento auth **auditato e atomico** (`append_audit(commit=False)` + un solo commit del servizio) con `details` whitelist. Deps nuove permissive: fastapi (MIT), uvicorn (BSD), argon2-cffi (MIT) | §6 ruoli/privilegio minimo; §7.2 utenza autorizzata dalla Direzione; §7.3 audit immutabile senza fughe; §3 open-source permissivo, prevenzione abusi. Review finale opus: nessun bypass/fuga; Ship with follow-ups |
| 2026-07-23 | Sott. 5: `append_audit` acquisisce `commit: bool = True` (default invariato per S2) → gli eventi auth partecipano alla **transazione unità-di-lavoro** del chiamante | Chiude i follow-up S2 «details da vincolare al confine operatore» e «transazione unità-di-lavoro» per gli eventi auth (§7.3: nessuna azione senza il suo record) |
| 2026-07-23 | Sott. 6 «Portale operatore — core matching»: **matching ibrido** = gate deterministico dei vincoli rigidi (disponibilità/turni-notturni/livello-lingua, enum) che esclude con motivo PRIMA dell'LLM + giudizio semantico LLM **ancorato** sul testo libero (per requisito: soddisfatto/no + evidenza citata dal profilo, constrained JSON, temp 0, fail-safe non-soddisfatto). Scoring = frazione soddisfatta (trasparente); gap → formazione consigliata. Richieste di lavoro (schema `matching`), ricerca profili (JSONB), endpoint operatore RBAC (attiva i permessi §6) + audit; matching on-demand, non persistito. Validato end-to-end con Qwen2.5-7B reale su GPU | §2 «mai una scatola nera» + §10 «matching spiegabile con gap»: spiegabile per costruzione senza tassonomia da mantenere; i vincoli non-negoziabili restano deterministici e non «ragionabili via» dal modello. Review finale opus: nessun giudizio sulla persona/rischio, no PII leak; Ship with follow-ups |
| 2026-07-23 | Sott. 6: nessuna tassonomia di competenze da mantenere — il matching semantico è affidato all'LLM locale (già disponibile, costo marginale zero) con output ancorato; alternativa scartata: tassonomia controllata (es. ESCO) che richiederebbe comunque un LLM per classificare il testo libero + manutenzione | Decisione di prodotto (utente): i profili sono testo libero in 5 lingue; un confronto per stringhe fallisce, e mantenere categorie è oneroso. L'ancoraggio + il gate deterministico preservano il §2 |
| 2026-07-23 | Sott. 7 «Voce»: servizi backend **STT = faster-whisper** (1.2.1, int8/CPU default, `device`/`compute_type` da env, `cuda` opt-in) + **TTS = Piper** (1.5.0, `synthesize_wav`), con **degrado elegante come contratto**: STT giù → `VoiceUnavailable` (si scrive), TTS assente/errore/lingua-senza-voce → `None` (si legge). Arabo-TTS **solo-testo di default** (mappa voci senza `ar`, §8); STT copre l'arabo. Import delle librerie pesanti **pigri** (unit con engine finto senza librerie); extra opzionale `[voice]`. Validato coi modelli reali (round-trip TTS→STT it/en quasi perfetto) | §3 testo-primo/voce-potenziamento/degrado elegante + open-source (faster-whisper MIT; **piper ~~MIT~~ → correzione 2026-07-28: `piper-tts` 1.x è GPL-3.0, eccezione §3 approvata, vedi §4.3/§14 e la riga di decisione del 2026-07-28**); §4 accessibilità; §7.1 ripiego voce↔testo; §8 arabo→testo. Review finale opus: contratto di degrado (lato fallimento) totale; Ship with follow-ups |
| 2026-07-24 | Sott. 8 «API rivolta alla persona (kiosk)»: primo layer HTTP rivolto alla persona — colloquio turno-per-turno (`/kiosk/interview/start|submit` sottili su `Interview` S4) + voce (`/kiosk/voice/transcribe|synthesize`) **con timeout** (`asyncio.wait_for`) → degrado `503`/`204`. **Sessione server-side** in-memory (registry che POSSIEDE la conn DB per l'intera sessione, TTL, chiusa all'eviction) + **token di dispositivo** `X-Kiosk-Token` (fail-closed, tempo costante). Topologia **single-box/localhost** (bind 127.0.0.1, no TLS, microfono via secure-context). Aggiunto `python-multipart` (Apache-2.0). Validato end-to-end via HTTP coi modelli reali (colloquio + round-trip voce) | §3 degrado elegante (mai un blocco) + prevenzione abusi (token) + localhost; §4 interrompibile; §7.1 colloquio/voce/kiosk. Il timeout voce **chiude il follow-up S7** «voce lenta → testo». Review finale opus: degrado/auth/lifecycle-conn corretti; Ship with follow-ups |
| 2026-07-24 | Sott. 9 «UI kiosk della persona (text-first)» — **primo frontend** (`frontend/`, React 18 + Vite 5 + TS + react-i18next). SPA che consuma l'API S8 turno-per-turno: lingua → consenso → colloquio → fine. **Macchina a stati degli schermi** (riduttore puro, nessun routing di URL; `step.kind`→schermo), `kioskClient` isolato con `X-Kiosk-Token` **build-time** e **degrado fail-closed** (401/404/5xx/rete/JSON-non-valido → schermo gentile, mai un throw o un blocco), i18n 5 lingue + **RTL arabo** alla radice, accessibilità (controllo dimensione testo, alto contrasto), **«Ferma» sempre montato in-sessione** che azzera, stato **in-memory** (privacy; nessun resume = Fase 2). **Voce = sottosistema successivo** (§3 testo-primo): controlli voce placeholder inerti. Stato `pending` (guardia richieste in volo + feedback «sto elaborando…») contro il doppio-invio su backend lento. Test: Vitest + @testing-library/react (54, gate verde, output pristine) | §3 testo-primo/degrado elegante/open-source permissivo/token kiosk; §4 consenso, interrompibilità, accessibilità; §8 5 lingue + RTL; §11 stringhe UI esternalizzate i18n. Review finale opus: **Ship with fixes** → corretti in-branch: guardia parse fail-closed (body 2xx non-JSON→unavailable), guardia in-flight/`pending`, e race «risposta tardiva annulla «Ferma»» (risposta stale ignorata quando `pending=false`). Follow-up §14: revisione arabo madrelingua, Playwright/axe, «opzioni rapide» §7.1 |
| 2026-07-24 | Sott. 10 «Voce del kiosk» — **strato voce** sul kiosk testo-first (S9), consuma l'API voce S8. `voiceClient` fail-closed (transcribe multipart→ok/unavailable, synthesize JSON→Blob/null; 503/204/rete/JSON-invalido mai un throw); hook `useSpeech` (riproduzione, coda-a-uno via `genRef`, revoca URL) e `useRecorder` (cattura MediaRecorder→transcribe, stati denied/unavailable); `VoiceProvider`/`useVoice` (lingua+muto+client); `VoiceBar` contestuale nelle schermate. **Lettura ad alta voce auto** su nuovo step + **muto** per-sessione + «Ascolta»; **dettatura** tocca-parla/stop → trascrizione **nel campo** per revisione (mai auto-invio, §5). **Degrado voce↔testo** su ogni fronte (503/204/permesso negato/autoplay bloccato → testo). API browser dietro seam mockabili; `voiceClient` iniettato via provider (default noop nei test). Macchina a stati del colloquio **intatta**. Test: Vitest + RTL (78, gate verde, pristine) | §3 testo-primo/voce-potenziamento/degrado elegante/open-source (API browser native, nessuna nuova dip); §4 accessibilità/inclusione (chi legge poco ascolta e parla); §7.1 voce + ripiego voce↔testo; §7.3 **il microfono non resta mai acceso** (3 race unmount/re-entrant chiuse); §8 arabo→testo per il TTS. Review finale opus: **Ship with fixes** → corretti in-branch: leak URL su stop, 3 race «mic-hot», coda-a-uno di `useSpeech` (synthesize concorrenti/tardivi + catch gen-guardato). Follow-up §14: **round-trip WebM reale** (top-rischio), try/catch costruttore MediaRecorder, voce TTS araba |
| 2026-07-25 | Sott. 11 «Portale operatore — Fondamenta + Auth» (sotto-progetto 1/5) — **primo pezzo del lato operatore**, **app React separata** in `operator-portal/` (Vite+TS+**react-router-dom** MIT+react-i18next; kiosk a `frontend/` INTATTO). Consuma l'API auth S5: login → **token Bearer in sessionStorage** (`operatorClient` isolato, fail-closed: 401→invalid/unauthorized, rete/5xx→error, mai un throw). **Gate «cambia password obbligatorio»** imposto dalla UI nel `ProtectedRoute` (**chiude il follow-up S5**: il server non lo impone) — non aggirabile, `mustChangePassword` riderivato da `/me` al reload. `AuthProvider` bootstrap sessione via `/me`; logout pulisce tutto; **avviso «sessione scaduta»** su 401 (flag, non al primo accesso); `me()` 'error' transient NON azzera il token. **Shell RBAC per ruolo** (§6, scheletro con voci disabilitate «in arrivo»; UX-only, il server resta l'autorità → 403). i18n italiano esternalizzato. Test: Vitest + RTL (30, gate verde+build, pristine). Nuova dip: react-router-dom (MIT); nessun segreto versionato | §2 accesso vincolato allo scopo (nulla senza login+ruolo); §3 locale/open-source permissivo/solo azioni previste; §6 ruoli a privilegio minimo; §7.2 utenza autorizzata; §7.3 resistenza abusi; §11 stringhe UI esternalizzate. Review finale opus: **Ship with fixes** → corretti: avviso sessione-scaduta renderizzato (era testo morto), token mantenuto su errore transient, aria-label nav i18n, test deep-link reale. App sibling (kiosk non ristrutturato). Follow-up §14: interceptor 401 globale, UX pwd-errata, roadmap sotto-progetti 2–5 |
| 2026-07-25 | Sott. 12 «Portale operatore — Richieste di lavoro + matching spiegabile» (sotto-progetto 2/5) — il **cuore del valore operatore** (§7.2), sullo scheletro S11, consuma l'API S6. `operatorClient` esteso con 4 metodi feature (list/get/create/runMatch) **fail-closed** (Bearer; 401→unauthorized, 403→forbidden, 404→not-found, else→error, mai un throw); `useApiError` centralizza 401→`onUnauthorized`+`/login` (**chiude il follow-up S11** per le chiamate feature). Sezioni: **lista** richieste (nav «Richieste» ora link reale via flag `built`), **form di creazione** (soli campi `JobRequestCreate` solo-lavoro, schema `extra="forbid"`), **dettaglio** con **«Esegui matching» a clic esplicito** (mai al mount; §10 latenza LLM) → **`MatchResults` spiegabile**: card espandibili con **frazione trasparente** (soddisfatti/totale), verdetti per-requisito **con evidenza citata**, **gap→formazione**, badge vincoli **derivato dai dati**. **Solo candidati compatibili** (l'endpoint S6 restituisce solo quelli — minimizzazione §5; nessuna lista esclusi, nessuna modifica backend). Pseudonimo opaco, nessuna PII (§5; evidenza già filtrata a monte S4/S6). Test: Vitest + RTL (47, gate verde+build, pristine); nessuna nuova dip; `frontend/` intatto | §2 «mai una scatola nera» + ambito solo-lavoro; §5 minimizzazione/pseudonimo; §6 ruolo operatore/RBAC (server resta l'autorità→403); §7.2 matching spiegabile con gap; §7.3 filtro PII a monte; §11 i18n. Review finale opus: **Ready to merge: Yes** (nessun Critical/Important); corretti in-branch: doppia-navigazione riga, badge vincoli hardcoded→derivato, test sezione debole→discriminante, dettaglio ora mostra tutti i campi solo-lavoro. Follow-up §14: roadmap sotto-progetti 3–5, coerenza unmount-guard, minori |
| 2026-07-28 | **Motore TTS Piper è GPL-3.0, non MIT — eccezione a §3 (nucleo) approvata dalla Direzione.** Emerso costruendo `CREDITS.md`: i metadati installati di `piper-tts` 1.5.0 dichiarano **GPL-3.0-or-later** (file `COPYING` = GNU GPL v3); è la riscrittura `OHF-Voice/piper1-gpl` (la fonemizzazione lega espeak-ng, GPL), non il vecchio `rhasspy/piper` MIT su cui si basava l'assunzione S7. Il codice lo importa **in-process** (`voice/tts.py`). GPL è copyleft → contraddice la clausola «permissiva» di §3 (nucleo protetto). **Decisione: accettare come eccezione documentata** (annotata in `CLAUDE.md` §3 a livello di principio + dettaglio in §4.3), perché GPL resta open/gratuito/locale e il pilota è **on-premise non distribuito** (obblighi copyleft attivati solo dalla distribuzione). Nessun refactor ora. STT (`faster-whisper`) e resto dello stack restano permissivi; le voci restano permissive | §3 «open source»/«budget nullo»/«locale» soddisfatti; la sola clausola «permissiva» ha un'eccezione motivata e reversibile. Governance §0 rispettata: stop → spiegazione → conferma esplicita utente → aggiornamento nucleo nello stesso passaggio. **Rivalutare se si distribuisce/commercializza:** isolare Piper come processo separato (arm's-length) o sostituire con motore permissivo |
| 2026-07-28 | Sott. 23 «Verifica licenze voci Piper (§3)» — chiude il **blocker pre-pilota** «licenze delle voci». Verificate dalle MODEL_CARD ufficiali `rhasspy/piper-voices` (HF): paola=CC0, davefx=CC0, siwis=CC-BY-4.0 (attribuzione), **lessac=research-only (Blizzard 2013) → NON permissiva**. Rimedio: sostituita la voce inglese **`en_US-lessac-medium` → `en_US-ljspeech-medium`** (LJ Speech public-domain, trained-from-scratch, **nessuna derivazione da lessac**) in `voice/config.py` + `fetch-voice-models.sh` + **test di regressione §3** che blocca il ritorno di lessac. Caveat trasversale «finetuning da lessac» per it/es/fr (dataset permissivi, pesi finetuned dalla base research-only): **rischio residuo ACCETTATO e documentato** (§14) — pilota non commerciale, dataset permissivi, pesi TTS trattati come non-derivati dalla comunità. Test: pytest (DB reale, **306 pass**, +1 regressione §3; ruff+mypy verdi). Solo `backend/` + `scripts/`; nessun cambio di comportamento osservabile (stessa lingua, stesso contratto voce §7/S8/S10 — solo il file voce inglese cambia, disponibilità TTS invariata). Follow-up §14: **attribuzione CC-BY di siwis (fr) da implementare prima del pilota** | §3 open-source **permissivo** (vincolo non negoziabile); §8 le lingue senza voce permissiva ripiegano su testo (non necessario: ljspeech è permissiva); §9 test a difesa dell'invariante. Verifica documentale (il codice non può imporre la licenza); decisione utente/Direzione registrata |
| 2026-07-28 | Sott. 22 «Consolidamento» — **refactor a comportamento invariato** (debito tecnico §14 accumulato in S15–S21; nessuna nuova funzionalità, contratto HTTP/RBAC/resa immutati). 4 dedup: (1) backend `SUPPORTED_LANGUAGES` → modulo neutro `bussola/languages.py` (dependency-free); `guardrails/refusal.py` re-export via `import…as…` (nome pubblico invariato, `test_refusal.py` intatto), `guardrails/pii.py` `list(...)` per Presidio, `system/service.py` import — stessi 5 valori/ordine. (2) frontend hook `useFetchOnMount(fetcher, onOk)` centralizza fetch-al-mount+degrado (401→login/403/error/loading gated) dei 3 pannelli supervisore/admin; fetcher **stabile via `useCallback([client])`** (deps ≡ originali, nessun re-fetch loop), `onOk` in ref. (3) frontend util `formatTimestamp` = `iso.replace('T',' ').slice(0,16)` **byte-identica** (AuditLog+OperatorActivityPanel; **niente TZ** — miglioramento a parte). (4) commento `nav.ts` aggiornato (flag `built?` **mantenuto**, scelta approvata). **Rete di sicurezza = suite esistenti verdi con asserzioni INVARIATE** (backend 305, frontend 131; solo test nuovi per le astrazioni). Solo `backend/`+`operator-portal/`; kiosk `frontend/` intatto; nessuna nuova dip/tabella | §3 open-source/solo azioni previste; §9 «non indebolire le garanzie» (refactor giustificato solo se dimostrabilmente neutro; TDD sulle astrazioni nuove); §11 codice inglese. **Nessuna linea rossa toccata** (nessun cambio sicurezza/RBAC/dati/comportamento). Review finale opus: **READY TO MERGE** (nessun Critical/Important; no re-fetch loop, backend fonte unica, nessun timestamp inline residuo, tutte le voci nav `built`, linee rosse intatte verificate al sorgente). Follow-up §14: TZ-formatter, PiiRedactor condiviso, `ruff format` nel gate, `.catch()` nell'hook |
| 2026-07-28 | Sott. 21 «Stato sistema / configurazione (Amministratore)» — **ultimo pannello del portale** (completa i 4 ruoli §6). Fetta verticale, **sola lettura**. Backend `bussola.system`: `compute_system_config(*, llm_reachable=…)` assembla la config **non-segreta** dai moduli `llm/auth/voice` (modello/endpoint-loopback/timeout, 5 lingue, STT, disponibilità TTS per lingua con ar=«solo testo», policy sessione) + un **check live LLM** (`GET {BASE_URL}/health`, timeout breve, **fail-safe** `except→False`) dietro **seam iniettabile** (test ermetici). `GET /system-config` dietro `require_permission(CONFIGURE_SYSTEM)` (solo **admin**→403), **auditato** (`system_config_viewed`). **Nessun segreto** nel DTO (il DSN/password di `data/config.py` non è importato; `SystemConfig` `extra="forbid"`); **nessun controllo di sicurezza** esposto/editabile (§2/§9). Frontend: `getSystemConfig()` fail-closed + `SystemConfigPanel` (nav «Configurazione»→link reale, `/config`): sezioni sola-lettura (LLM+badge raggiungibilità, lingue, voce, sessione). Test: pytest (DB reale) + Vitest/RTL (127). Solo `operator-portal/`+`backend/`; kiosk intatto; nessuna nuova dip/tabella | §2/§9 nessun controllo di sicurezza esposto né indebolibile (guardrail/ambito/PII fuori dal DTO, sola lettura); §3/§7.3 nessun segreto in uscita; §6 admin CONFIGURE_SYSTEM «cura il funzionamento», server autorità (403); §7.3 accesso auditato. Review finale opus: **Ready to merge: Yes** (linee rosse verificate al sorgente; nessun Critical/Important). **Portale operatore completo (4 ruoli §6).** Follow-up §14: chore di consolidamento (commento nav, dedup SUPPORTED_LANGUAGES, hook fetch condiviso, formatter date); env-docs credenziali LLM URL |
| 2026-07-28 | Sott. 20 «Attività operatori (Supervisore)» — fetta verticale (come S15). Backend `bussola.activity`: `compute_operator_activity(conn)` **aggrega** `audit.audit_log` per `actor` con `COUNT(*) FILTER` sulle **5 azioni di lavoro** (`profile_viewed`/`profiles_searched`/`matching_run`/`export_requested`/`export_downloaded`) + `MAX(occurred_at)`, `WHERE actor IS NOT NULL AND action = ANY(...)` (esclude per costruzione kiosk/auth/admin/supervisore), ordinato per last_active DESC. `GET /operator-activity` dietro `require_permission(VIEW_OPERATOR_ACTIVITY)` (solo **supervisore** → 403 altri), **auditato** (`operator_activity_viewed`). Frontend: `getOperatorActivity()` fail-closed + `OperatorActivityPanel` (nav «Attività operatori» → link reale, rotta `/activity`): tabella per-operatore sola-lettura (username + conteggi + ultima attività). **Aggregato staff-only**, nessuno pseudonimo/PII/dato detenuto. Test: pytest (DB reale) + Vitest/RTL (120). Solo `operator-portal/`+`backend/`; kiosk intatto; nessuna nuova dip/tabella | §2/§5 misura l'attività dello **staff** per il coordinamento, **non** sorveglianza sulle persone (aggregato, no dati detenuto); §3 fail-closed/SQL parametrizzato/i18n; §6 supervisore «stato di avanzamento/organizza il lavoro», privilegio minimo, **distinto dall'Auditor** (riepilogo, non log grezzo né tamper-verify), server autorità (403); §7.3 accesso auditato. Review finale opus: **Ready to merge: Yes** (3 linee rosse verificate al sorgente; nessun Critical/Important); ripiegata l'invariante «per-azione-non-per-ruolo» (commento + test esclusione). **Ruolo Supervisore concluso.** Follow-up §14: solo admin-config; formatter date condiviso |
| 2026-07-27 | Sott. 19 «Vista del log di audit (Auditor) — Frontend» — il **visore** del log per l'auditor sul contratto S18. `operatorClient` esteso: `listAudit(filters)` (omette filtri vuoti; cursore `before`/`limit`) + `verifyAudit()`, fail-closed. Schermata `AuditLog` (nav «Log di audit» → link reale, rotta `/audit`, ruolo auditor): pannello filtri (attore/azione/da-a), tabella voci (data/attore/azione/pseudonimo«—»/`details` compatti via `detailsSummary`), **«Carica altri»** (cursore `before`=ultimo id, pagina i **filtri applicati** non quelli live, `hasMore` su page==LIMIT=50, append, sparisce a fine log), **«Verifica integrità» su richiesta** → badge verde «Catena integra» / rosso «Manomissione rilevata alla riga N — reason». Guard in-flight sui bottoni (`busy`). Sola lettura (nessuna azione mutante). Test: Vitest+RTL (113). Solo `operator-portal/`; kiosk+backend intatti; nessuna nuova dip | §2 accountability, **non** sorveglianza (azioni operatori + pseudonimi opachi, nessuna PII/inferenza); §3 fail-closed/i18n; §6 auditor sola-lettura, server autorità (403; rotta auth-gated, nav per-ruolo); §7.3 tamper-evidence resa verificabile (badge). Review finale opus: **Ready to merge: Yes** (4 linee rosse verificate; nessun Critical/Important); ripiegati guard in-flight + reason del tamper. **Ruolo Auditor concluso.** Follow-up §14: attività-operatori supervisore, admin-config; timestamp TZ; test 401 |
| 2026-07-27 | Sott. 18 «Vista del log di audit (Auditor) — Backend» — rende **leggibile** dal sistema il registro di audit (S2, append-only hash-chained) per il ruolo **Auditor** (§6 «garantisce il corretto uso, sola lettura»). `data/audit.py`: `AuditEntry` (vista sola-lettura: id/occurred_at/actor/action/target_pseudonym/details, **niente hash interni**, `extra="forbid"`) + `list_audit` (SELECT-only, cursore per `id DESC` con `before`, `limit` cap 1..200, filtri parametrizzati actor/action/from-to); riusa `verify_audit_chain`. Router `GET /audit` + `GET /audit/verify` dietro `require_permission(READ_AUDIT)` (**solo auditor** → 403 tutti gli altri). **Letture pure**: nessun evento appeso (§6 «non modifica nulla», test conteggio invariato). Nessuna nuova tabella/migrazione. Test: pytest (DB reale, 290 pass). Solo `backend/`; `operator-portal/`+`frontend/` intatti; nessuna nuova dip | §2 accountability contro il riuso improprio, **non** sorveglianza (traccia azioni operatori + pseudonimi opachi, mai dati/inferenze sulla persona); §3 fail-closed/SQL parametrizzato/solo azioni previste; §6 auditor sola-lettura/privilegio minimo, RBAC applicativo (server autorità→403); §7.3 registro immutabile + **tamper-evidence resa verificabile** (`/audit/verify`). Review finale opus: **Ready to merge: Yes** (nessun Critical/Important; RBAC solo-auditor e invariante read-only verificati). Follow-up §14: auditor-frontend; read path via `bussola_app` vs `bussola_auditor` (decisione §3.6, hardening futuro); Query bounds; verifica catena incrementale |
| 2026-07-27 | Sott. 17 «Export con autorizzazione — Frontend» (sotto-progetto 5b/5, parte frontend) — le due superfici del portale sul contratto `/exports` (S16). `operatorClient` esteso con 6 metodi fail-closed (`createExport`/`listExports`/`listPendingExports`/`approveExport`/`denyExport`/`downloadExport`); il download restituisce un **Blob** e la UI salva `export-{id}.json` (mai anteprima a schermo). **Operatore** (`/export`, nav→built): «Le mie richieste» (badge stato) + «Nuova richiesta» (filtri S13 + **motivo obbligatorio**) + **Download solo su `approved`** (seam `saveBlob` mockabile). **Supervisore** (nuova `/export-approvals`, nav→built): **coda pending** metadati-only (richiedente, filtri leggibili con **«Tutti i profili»** se vuoti, motivo, data) + **Approva** con conferma (ConfirmDialog S14, contesto who/scope/reason) / **Nega** con motivo obbligatorio (DenyDialog). Test: Vitest+RTL (101). Solo `operator-portal/`; kiosk `frontend/` + `backend/` intatti; nessuna nuova dip | §2/§5 nessun dato di profilo/PII reso (payload = solo file; tipo `ExportRequest` = metadati → fuga strutturalmente impossibile); §3 fail-closed/solo azioni previste/i18n; §6 richiedente operatore / approvatore supervisore, server autorità; §7.2 esportazione di base + spiegabilità (l'approvatore vede scope+motivo); §7.3 **la UI NON è il gate** (409/404 dal server = degrado, mai dati; Download-su-approved = comodità). Review finale opus: **Ready to merge: With fixes (minori)** — nessun Critical/Important, linee rosse verificate; ripiegati 2 fix sul dialog di approvazione (pin del contenuto + `white-space: pre-line`). Follow-up §14: ciclo operatore concluso; classe footgun label-nav==titolo; attività-operatori/auditor/admin-config restano |
| 2026-07-27 | Sott. 16 «Export con autorizzazione — Backend» (sotto-progetto 5b/5, parte backend) — il canale **più sensibile** (unica uscita dati, §7.3). Migrazione additiva `0006` (schema `export` + `export_request`: filtri jsonb, motivo, `status` con CHECK, tracce decisione; grant `bussola_app` SELECT/INSERT/UPDATE **no-DELETE**; auditor senza accesso). `bussola.export`: `ExportService` a stati **pending→approved|denied**, transizioni **guardate a DB** (`UPDATE … WHERE status='pending'` → 404/409, race-safe), **generazione on-demand** al download (ri-esegue `ProfileRepository.search(filtri)` → `list[WorkProfile]`; **nessun payload memorizzato**), **audit atomico** di ogni azione (`export_requested/approved/denied/downloaded`, filtri-names+count, **no PII**). Endpoint `/exports`: crea/lista/download (`EXPORT_DATA`, operatore), pending/approva/nega (**nuovo `APPROVE_EXPORTS`**, supervisore) — **download gated dal server** (solo `approved` + propria → 404/409). Ambito = filtri S13, formato JSON. Test: pytest (DB reale, 280 pass). Solo `backend/`; `operator-portal/`+`frontend/` intatti; nessuna nuova dip | §2/§5 payload solo-lavoro/pseudonimo (whitelist `extra="forbid"` + `PiiRedactor`, nessuna mappa persona); §3 fail-closed/solo azioni previste/flusso autorizzativo per condivisioni esterne; §6 richiedente operatore / approvatore supervisore, **ruoli disgiunti → no auto-approvazione**, server autorità; §7.2 esportazione di base (estrazione controllata); §7.3 **autorizzazione obbligatoria imposta dal server** + filtro PII in uscita + audit immutabile; §8 esiti matching/CSV/reportistica = Fase 2. Review finale opus: **Ready to merge: Yes** (nessun Critical/Important; gate download non aggirabile + payload solo-WorkProfile verificati). Follow-up §14: 5b-frontend, redactor condiviso (trasversale), visibilità «tutti i profili» all'approvatore |
| 2026-07-27 | Sott. 15 «Portale operatore — Metriche di qualità» (sotto-progetto 5a/5) — **prima fetta verticale** (backend nuovo + frontend). Backend: modulo `bussola.metrics` (`compute_metrics(conn) -> Metrics`, letture **aggregate** da `profiles.work_profile`/`matching.job_request`/`audit.audit_log`, **nessuna nuova tabella**; completezza = frazione di **5 sezioni-chiave** popolate — lingue/competenze/esperienze/formazione + aspirazioni) + `GET /metrics` dietro `require_permission(VIEW_METRICS)` (solo **supervisore** → 403 altri) con `append_audit("metrics_viewed")`. Metriche: `total_profiles`, `completed_profiles` (completezza 100%), `average_completeness`, `total_job_requests`, `matching_runs`. **Aggregato e anonimo** per costruzione (§2/§5: nessuno pseudonimo/dato per-persona; `Metrics` con `extra="forbid"`). Frontend: `operatorClient.getMetrics()` fail-closed + `MetricsPanel` supervisore sola-lettura (card + completezza come %, degrado 401/403/error, loading gated `!error`); nav «Metriche» ora link reale (`built`), rotta `/metrics`. **«Colloqui completati» derivato dal profilo → nessuna modifica a S4/S8** (regola del nucleo §0). Test: pytest (backend, DB reale) + Vitest/RTL (82). Nessuna nuova dip; kiosk `frontend/` intatto | §2/§5 metriche misurano il sistema, non le persone (aggregato/anonimo); §6 supervisore «vede le metriche di qualità»/privilegio minimo (server = autorità→403); §7.2 metriche minime (n. colloqui completati + completezza); §7.3 accesso auditato; §8 reportistica avanzata = Fase 2; §11 i18n. Review finale opus: **Ready to merge: Yes** (nessun Critical/Important); ripiegati 2 hardening on-theme (`_TOTAL_SECTIONS` derivato + `Metrics` `extra="forbid"`, fix `5b2423a`). Follow-up §14: 5b export, attività operatori/config/audit, calcolo SQL-side se cresce la popolazione |
| 2026-07-27 | Sott. 14 «Portale operatore — Amministrazione utenze» (sotto-progetto 4/5) — il pannello dell'**Amministratore** (§6), sullo scheletro S11/pattern S12–S13, consuma l'API S5 `operators` (permesso `MANAGE_OPERATORS` → solo `admin`; server = autorità → 403). `operatorClient` esteso con 5 metodi **fail-closed** (list/create/disable/enable/reset; Bearer; 401→unauthorized, 403→forbidden, else→error). Sezione `/operators` (nav «Gestione utenze» ora link reale via `built`, rotta **solo auth-gated**): **elenco** (utente/nome/ruolo via `shell.role`/stato/badge «deve cambiare pwd»), **crea** (soli 3 campi `CreateOperatorRequest`), **disabilita/riabilita** e **reset-password**. **Password temporanea in modale una-tantum** (crea + reset): copy dietro seam, «Copiato» solo su copia riuscita, **mai persistita né loggata**, stato azzerato alla chiusura. **Conferma** (con nome) prima di disable/enable/reset; **guardia anti-auto-lockout** sulla riga dell'admin loggato (disable+reset disabilitati, il backend non ha guardia propria → revoca sessioni = auto-lockout). Ambito entro gli endpoint esistenti: **no modifica/cancellazione** (disable = rimozione reversibile, §7.3). Nessun dato solo-lavoro/PII (solo account). Test: Vitest + RTL (75, gate verde+build, pristine); nessuna nuova dip; `frontend/` intatto | §2 solo azioni previste/nessun dato non pertinente; §3 fail-closed/prevenzione abusi; §6 ruolo Amministratore «gestisce la piattaforma, non il merito»/privilegio minimo (server = autorità→403); §7.2 utenza autorizzata dalla Direzione; §7.3 audit (ogni azione emette evento S5), no cancellazione. Review finale opus: **Ready to merge: Yes** (nessun Critical/Important); ripiegato in-branch il minor «Copiato» falso-positivo su clipboard assente (fix `b294ea7`). Follow-up §14: sotto-progetto 5 (metriche+export, follow-on backend), modifica/cancellazione `operators` (follow-on), focus-trap/Escape modali |
| 2026-07-26 | Sott. 13 «Portale operatore — Consultazione profili» (sotto-progetto 3/5) — completa il ciclo di consultazione (§7.2), sullo scheletro S11/pattern S12, consuma l'API S6 (`GET /profiles?availability=&language=&note=&skill_query=`, `GET /profiles/{pseudonym}`). `operatorClient` esteso con `searchProfiles`/`getProfile` **fail-closed** (Bearer; 401→unauthorized, 403→forbidden, 404 (get)→not-found, else→error); `useApiError` per il 401. **Sezione «Profili»** (nav ora link reale via `built`): **ricerca/filtri** (disponibilità/lingua/nota-operativa/competenza; i filtri vuoti omessi dalla query; **ricerca senza filtri = lista completa permessa e auditata** lato S6, §7.3 — l'enumerazione è tracciata, non nascosta) + **dettaglio profilo solo-lavoro** (pseudonimo opaco, alfabetizzazione digitale, competenze con kind + **grado di evidenza a etichetta+colore+icona** Certificata/Dimostrata/Dichiarata, lingue+livello, esperienze, aspirazioni, formazione, note). **Consult-only**: nessun export (→ sotto-progetto 5) né modifica (i profili li conferma la persona, §5). Nessuna PII (solo pseudonimo + dati solo-lavoro; free-text già filtrata a monte S4/S6). i18n italiano incl. mappe di etichette enum. Test: Vitest + RTL (59, gate verde+build, pristine); nessuna nuova dip; `frontend/` intatto | §2 ambito solo-lavoro; §5 minimizzazione/pseudonimo/grado di evidenza; §6 ruolo operatore/RBAC (server resta l'autorità→403); §7.2 «ricerca e filtri sui profili» + «trovare le persone adatte»; §7.3 audit dell'enumerazione, no export non autorizzato, filtro PII a monte; §11 i18n. Review finale opus: **Ready to merge — with fixes**; corretto in-branch (fix `2f9aedd`): ProfileSearch non mostra più un «Caricamento…» perenne sotto un errore al mount (guard `profiles===null && !error` + test di regressione), reset stato in ProfileDetail al cambio pseudonimo, contrasto badge «Dichiarata» ad AA. Follow-up §14: roadmap sotto-progetti 4–5, rotte solo auth-gated, minori |
