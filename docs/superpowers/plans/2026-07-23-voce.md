# Piano — Sottosistema 7: Voce (STT + TTS)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Servizi backend Speech-to-Text (faster-whisper) e Text-to-Speech (Piper) su CPU, con degrado elegante voce→testo per contratto, multilingua, e voci a licenza permissiva.

**Architecture:** Package `bussola.voice`: `SpeechToText`/`TextToSpeech` incapsulano un *engine* iniettabile; la logica del **contratto di degrado** (VoiceUnavailable / None) è testata con engine finti (deterministici, senza librerie pesanti); gli engine reali (faster-whisper, Piper) importano le librerie **pigramente** e sono esercitati solo dal test d'integrazione `requires_voice`. Nessun HTTP, nessun accoppiamento col colloquio.

**Tech Stack:** Python 3.12, faster-whisper (MIT, CTranslate2), piper-tts (MIT), Pydantic, pytest. Dipendenze voce in un **extra opzionale** `[voice]` (pesanti/platform-sensitive — non richieste per gli unit).

## Global Constraints

- **Prima il testo, la voce come potenziamento, degrado elegante** (§3/§7.1): il contratto non deve MAI far bloccare. **STT** giù/errore → `VoiceUnavailable` (il chiamante scrive). **TTS** assente/errore/lingua-senza-voce → **`None`** (il chiamante legge). Il testo funziona sempre.
- **Arabo-TTS = solo-testo di default** (§8): la mappa voci NON contiene `ar` di default → `synthesize(..., "ar")` ritorna `None` finché non validato sul pilota.
- **Licenze permissive** (§3): dipendenze MIT; **voci Piper solo a licenza permissiva** (vetting per-voce, annotate).
- **Device STT configurabile** (§5): `cpu`/`int8` di default; `cuda` attivabile via env.
- **Import pigri delle librerie pesanti** dentro gli engine reali: gli unit (engine finto) NON devono richiedere faster-whisper/piper installati; l'integrazione reale si **skippa** se librerie/modelli sono assenti.
- **TDD; solo dati sintetici** (§9); **codice in inglese** (§11).
- **Gate:** `ruff check` + `ruff format --check` (file toccati) + `mypy --strict` su `backend/src` puliti.
- **Shell state non persiste tra chiamate Bash:** percorsi assoluti `backend/.venv/bin/...` dalla radice del repo; niente `cd`.

---

## Struttura dei file

```
backend/
├── pyproject.toml                 # + [project.optional-dependencies] voice = [faster-whisper, piper-tts]
└── src/bussola/voice/
    ├── __init__.py
    ├── config.py                  # tunables da env: STT model/device/compute, dir modelli, mappa lingua→voce (no ar)
    ├── errors.py                  # VoiceUnavailable
    ├── models.py                  # Transcription
    ├── stt.py                     # SpeechToText + SttEngine (Protocol) + _FasterWhisperEngine (lazy)
    └── tts.py                     # TextToSpeech + TtsEngine (Protocol) + _PiperEngine (lazy)
scripts/
└── fetch-voice-models.sh          # scarica una volta il modello STT + le voci Piper permissive
backend/tests/voice/
├── __init__.py
├── test_config.py
├── test_stt.py                    # contratto STT con engine finto
├── test_tts.py                    # contratto TTS con engine finto
└── test_voice_live.py             # round-trip TTS→STT (requires_voice) — lo avvio io
```

---

### Task 1: Fondamenta — dipendenze `[voice]` + `config.py` + `errors.py` + `models.py`

**Files:**
- Modify: `backend/pyproject.toml` (aggiunge l'extra `voice`)
- Create: `backend/src/bussola/voice/__init__.py` (vuoto), `config.py`, `errors.py`, `models.py`
- Test: `backend/tests/voice/__init__.py` (vuoto), `backend/tests/voice/test_config.py`

**Interfaces:**
- Produces:
  - `config`: `STT_MODEL` (default `"large-v3-turbo"`), `STT_DEVICE` (default `"cpu"`), `STT_COMPUTE_TYPE` (default `"int8"`), `VOICE_MODEL_DIR` (default `"models/voice"`), `PIPER_VOICES: dict[str, str]` (chiavi `it/en/fr/es`; **`ar` assente**).
  - `errors.VoiceUnavailable(Exception)`.
  - `models.Transcription(text: str, language: str)` (Pydantic `extra="forbid"`).

- [ ] **Step 1: Aggiungere l'extra voce**

In `backend/pyproject.toml`, dopo il blocco `dev = [...]` dentro `[project.optional-dependencies]`, aggiungere:
```toml
voice = [
    "faster-whisper>=1.0,<2",
    "piper-tts>=1.2,<2",
]
```
(NON installarlo ora: gli unit di questo task usano un engine finto e non ne hanno bisogno. Si installa al Task 4, per l'integrazione reale.)

**Inoltre**, poiché gli engine reali (Task 2/3) importano `faster_whisper`/`piper` (senza stub e non installati nell'ambiente di gate), aggiungere questi moduli all'override mypy esistente perché `mypy --strict` non fallisca con «cannot find stubs». In `[[tool.mypy.overrides]]`, estendere la lista `module` (che già contiene `presidio_analyzer.*`, `presidio_anonymizer.*`, `spacy.*`) con `"faster_whisper.*"` e `"piper.*"`:
```toml
[[tool.mypy.overrides]]
module = ["presidio_analyzer.*", "presidio_anonymizer.*", "spacy.*", "faster_whisper.*", "piper.*"]
ignore_missing_imports = true
```

- [ ] **Step 2: Scrivere il test (fallisce)**

File `backend/tests/voice/__init__.py`: (vuoto)

File `backend/tests/voice/test_config.py`:
```python
from bussola.voice import config
from bussola.voice.errors import VoiceUnavailable
from bussola.voice.models import Transcription


def test_stt_defaults_are_cpu_int8():
    assert config.STT_DEVICE == "cpu"
    assert config.STT_COMPUTE_TYPE == "int8"
    assert config.STT_MODEL  # non-empty


def test_piper_voices_cover_four_languages_without_arabic():
    assert set(config.PIPER_VOICES) == {"it", "en", "fr", "es"}
    assert "ar" not in config.PIPER_VOICES  # Arabic TTS falls back to text by default


def test_voice_unavailable_is_exception():
    assert issubclass(VoiceUnavailable, Exception)


def test_transcription_forbids_extra_fields():
    import pytest
    from pydantic import ValidationError

    Transcription(text="ciao", language="it")
    with pytest.raises(ValidationError):
        Transcription(text="ciao", language="it", speaker="x")
```

- [ ] **Step 3: Eseguire (deve fallire)** — `backend/.venv/bin/pytest backend/tests/voice/test_config.py -q` → FAIL (modulo inesistente).

- [ ] **Step 4: Implementare**

File `backend/src/bussola/voice/__init__.py`: (vuoto)

File `backend/src/bussola/voice/config.py`:
```python
"""Voice tunables, from environment with safe defaults.

STT runs on CPU by default so the 8 GB GPU stays fully with the LLM
(STATO_TECNICO §5); `device`/`compute_type` are env-overridable to `cuda`
when VRAM allows. The Piper voice map intentionally omits Arabic: Arabic TTS
falls back to text until a permissive, adequate voice is validated (§8)."""

from __future__ import annotations

import os

from bussola.env import load_project_dotenv

load_project_dotenv()

STT_MODEL = os.environ.get("BUSSOLA_STT_MODEL", "large-v3-turbo")
STT_DEVICE = os.environ.get("BUSSOLA_STT_DEVICE", "cpu")
STT_COMPUTE_TYPE = os.environ.get("BUSSOLA_STT_COMPUTE_TYPE", "int8")
VOICE_MODEL_DIR = os.environ.get("BUSSOLA_VOICE_MODEL_DIR", "models/voice")

# language -> Piper voice model filename (resolved under VOICE_MODEL_DIR).
# Only permissively-licensed voices (§3). Arabic is intentionally absent
# (text fallback) until validated on the pilot (§8).
PIPER_VOICES: dict[str, str] = {
    "it": os.environ.get("BUSSOLA_PIPER_VOICE_IT", "it_IT-paola-medium.onnx"),
    "en": os.environ.get("BUSSOLA_PIPER_VOICE_EN", "en_US-lessac-medium.onnx"),
    "fr": os.environ.get("BUSSOLA_PIPER_VOICE_FR", "fr_FR-siwis-medium.onnx"),
    "es": os.environ.get("BUSSOLA_PIPER_VOICE_ES", "es_ES-davefx-medium.onnx"),
}
```

File `backend/src/bussola/voice/errors.py`:
```python
"""Voice domain errors."""

from __future__ import annotations


class VoiceUnavailable(Exception):
    """STT could not produce a transcription; the caller falls back to text."""
```

File `backend/src/bussola/voice/models.py`:
```python
"""Voice domain models."""

from __future__ import annotations

from pydantic import BaseModel, ConfigDict


class Transcription(BaseModel):
    model_config = ConfigDict(extra="forbid", str_strip_whitespace=True)

    text: str
    language: str
```

- [ ] **Step 5: Eseguire (devono passare)** — PASS (4 test). Full suite: `backend/.venv/bin/pytest backend/tests -q`.

- [ ] **Step 6: Committare**
```bash
git add backend/pyproject.toml backend/src/bussola/voice/__init__.py backend/src/bussola/voice/config.py backend/src/bussola/voice/errors.py backend/src/bussola/voice/models.py backend/tests/voice/__init__.py backend/tests/voice/test_config.py
git commit -m "feat(voice): fondamenta — config (device CPU, no arabo TTS) + errori + modelli"
```

---

### Task 2: `SpeechToText` — contratto di degrado (`stt.py`)

**Files:**
- Create: `backend/src/bussola/voice/stt.py`
- Test: `backend/tests/voice/test_stt.py`

**Interfaces:**
- Consumes: `config`, `errors.VoiceUnavailable`, `models.Transcription`.
- Produces:
  - `SttEngine(Protocol)` con `transcribe(audio: bytes, language: str) -> str`.
  - `SpeechToText(engine: SttEngine | None = None)` con `transcribe(audio: bytes, language: str) -> Transcription`; su QUALSIASI errore dell'engine → `VoiceUnavailable`. Se `engine is None`, costruisce pigramente `_FasterWhisperEngine` (import di `faster_whisper` dentro il costruttore dell'engine, non a livello di modulo).
  - `_FasterWhisperEngine` (engine reale, lazy) — esercitato solo dal test `requires_voice` (Task 4).

- [ ] **Step 1: Scrivere il test (fallisce)**

File `backend/tests/voice/test_stt.py`:
```python
import pytest

from bussola.voice.errors import VoiceUnavailable
from bussola.voice.models import Transcription
from bussola.voice.stt import SpeechToText


class FakeStt:
    def __init__(self, *, text: str = "", raises: bool = False) -> None:
        self._text = text
        self._raises = raises
        self.calls: list[tuple[bytes, str]] = []

    def transcribe(self, audio: bytes, language: str) -> str:
        self.calls.append((audio, language))
        if self._raises:
            raise RuntimeError("engine down")
        return self._text


def test_transcribe_returns_transcription():
    engine = FakeStt(text="so cucinare")
    result = SpeechToText(engine=engine).transcribe(b"AUDIO", "it")
    assert isinstance(result, Transcription)
    assert result.text == "so cucinare"
    assert result.language == "it"


def test_language_hint_is_propagated_to_engine():
    engine = FakeStt(text="hello")
    SpeechToText(engine=engine).transcribe(b"AUDIO", "en")
    assert engine.calls == [(b"AUDIO", "en")]


def test_engine_failure_raises_voice_unavailable():
    with pytest.raises(VoiceUnavailable):
        SpeechToText(engine=FakeStt(raises=True)).transcribe(b"AUDIO", "it")
```

- [ ] **Step 2: Eseguire (deve fallire)** — FAIL (modulo inesistente).

- [ ] **Step 3: Implementare**

File `backend/src/bussola/voice/stt.py`:
```python
"""Speech-to-Text service. The degradation contract lives here: ANY engine
failure becomes `VoiceUnavailable`, so the caller falls back to text (§3/§7.1).
The real faster-whisper engine imports the library lazily, so this module and
its unit tests do not require faster-whisper to be installed."""

from __future__ import annotations

import io
from typing import Protocol

from bussola.voice import config
from bussola.voice.errors import VoiceUnavailable
from bussola.voice.models import Transcription


class SttEngine(Protocol):
    def transcribe(self, audio: bytes, language: str) -> str: ...


class SpeechToText:
    def __init__(self, engine: SttEngine | None = None) -> None:
        self._engine = engine

    def _get_engine(self) -> SttEngine:
        if self._engine is None:
            self._engine = _FasterWhisperEngine()
        return self._engine

    def transcribe(self, audio: bytes, language: str) -> Transcription:
        try:
            text = self._get_engine().transcribe(audio, language)
        except Exception as exc:  # degradation net: voice never blocks (§3)
            raise VoiceUnavailable(str(exc)) from exc
        return Transcription(text=text, language=language)


class _FasterWhisperEngine:
    """Real STT engine (faster-whisper, CTranslate2). Imported lazily.

    NOTE: verify the transcribe signature against the installed faster-whisper
    version; the `requires_voice` round-trip test (Task 4) is the check."""

    def __init__(self) -> None:
        from faster_whisper import WhisperModel

        self._model = WhisperModel(
            config.STT_MODEL, device=config.STT_DEVICE, compute_type=config.STT_COMPUTE_TYPE
        )

    def transcribe(self, audio: bytes, language: str) -> str:
        segments, _info = self._model.transcribe(io.BytesIO(audio), language=language)
        return "".join(segment.text for segment in segments).strip()
```

- [ ] **Step 4: Eseguire (devono passare)** — PASS (3 test).

- [ ] **Step 5: Committare**
```bash
git add backend/src/bussola/voice/stt.py backend/tests/voice/test_stt.py
git commit -m "feat(voice): SpeechToText con contratto di degrado (VoiceUnavailable) + engine lazy"
```

---

### Task 3: `TextToSpeech` — contratto di degrado (`tts.py`)

**Files:**
- Create: `backend/src/bussola/voice/tts.py`
- Test: `backend/tests/voice/test_tts.py`

**Interfaces:**
- Consumes: `config`.
- Produces:
  - `TtsEngine(Protocol)` con `synthesize(text: str, voice_model: str) -> bytes`.
  - `TextToSpeech(engine: TtsEngine | None = None, voices: dict[str, str] | None = None)` con `synthesize(text: str, language: str) -> bytes | None`: se la lingua non ha voce (es. `ar`) → `None`; su QUALSIASI errore dell'engine → `None`; altrimenti i byte WAV. `voices` default = `config.PIPER_VOICES`. Se `engine is None`, costruisce pigramente `_PiperEngine`.
  - `_PiperEngine` (engine reale, lazy) — esercitato solo dal test `requires_voice` (Task 4).

- [ ] **Step 1: Scrivere il test (fallisce)**

File `backend/tests/voice/test_tts.py`:
```python
from bussola.voice.tts import TextToSpeech

_VOICES = {"it": "it.onnx", "en": "en.onnx"}


class FakeTts:
    def __init__(self, *, raises: bool = False) -> None:
        self._raises = raises
        self.calls: list[tuple[str, str]] = []

    def synthesize(self, text: str, voice_model: str) -> bytes:
        self.calls.append((text, voice_model))
        if self._raises:
            raise RuntimeError("tts down")
        return b"WAVDATA"


def test_synthesize_returns_audio_bytes():
    tts = TextToSpeech(engine=FakeTts(), voices=_VOICES)
    assert tts.synthesize("ciao", "it") == b"WAVDATA"


def test_language_without_voice_returns_none():
    # Arabic has no voice by default -> text fallback (None), no engine call
    engine = FakeTts()
    tts = TextToSpeech(engine=engine, voices=_VOICES)
    assert tts.synthesize("مرحبا", "ar") is None
    assert engine.calls == []


def test_engine_failure_returns_none():
    tts = TextToSpeech(engine=FakeTts(raises=True), voices=_VOICES)
    assert tts.synthesize("ciao", "it") is None


def test_selects_voice_model_for_language():
    engine = FakeTts()
    TextToSpeech(engine=engine, voices=_VOICES).synthesize("hello", "en")
    assert engine.calls == [("hello", "en.onnx")]
```

- [ ] **Step 2: Eseguire (deve fallire)** — FAIL.

- [ ] **Step 3: Implementare**

File `backend/src/bussola/voice/tts.py`:
```python
"""Text-to-Speech service. Absence of audio is a NORMAL fallback, not an error:
a language with no configured voice (Arabic by default, §8) or any engine
failure yields `None`, so the caller shows text (§3/§7.1). The real Piper engine
imports the library lazily, so this module and its unit tests do not require
piper-tts to be installed."""

from __future__ import annotations

import io
import wave
from typing import Protocol

from bussola.voice import config


class TtsEngine(Protocol):
    def synthesize(self, text: str, voice_model: str) -> bytes: ...


class TextToSpeech:
    def __init__(
        self, engine: TtsEngine | None = None, voices: dict[str, str] | None = None
    ) -> None:
        self._engine = engine
        self._voices = voices if voices is not None else config.PIPER_VOICES

    def _get_engine(self) -> TtsEngine:
        if self._engine is None:
            self._engine = _PiperEngine()
        return self._engine

    def synthesize(self, text: str, language: str) -> bytes | None:
        voice_model = self._voices.get(language)
        if voice_model is None:
            return None  # no voice for this language (e.g. Arabic) -> text fallback
        try:
            return self._get_engine().synthesize(text, voice_model)
        except Exception:  # TTS failure -> text fallback, never blocks (§3)
            return None


class _PiperEngine:
    """Real TTS engine (Piper). Imported lazily; loaded voices are cached.

    NOTE: verify the synthesize signature against the installed piper-tts
    version; the `requires_voice` round-trip test (Task 4) is the check."""

    def __init__(self) -> None:
        self._loaded: dict[str, object] = {}

    def synthesize(self, text: str, voice_model: str) -> bytes:
        voice = self._load(voice_model)
        buffer = io.BytesIO()
        with wave.open(buffer, "wb") as wav_file:
            voice.synthesize(text, wav_file)  # type: ignore[attr-defined]
        return buffer.getvalue()

    def _load(self, voice_model: str) -> object:
        if voice_model not in self._loaded:
            import os

            from piper import PiperVoice

            self._loaded[voice_model] = PiperVoice.load(
                os.path.join(config.VOICE_MODEL_DIR, voice_model)
            )
        return self._loaded[voice_model]
```

- [ ] **Step 4: Eseguire (devono passare)** — PASS (4 test). Full suite: `backend/.venv/bin/pytest backend/tests -q`.

- [ ] **Step 5: Committare**
```bash
git add backend/src/bussola/voice/tts.py backend/tests/voice/test_tts.py
git commit -m "feat(voice): TextToSpeech con ripiego a testo (None) + engine Piper lazy"
```

---

### Task 4: Provisioning + integrazione round-trip reale (`requires_voice`)

**Files:**
- Create: `scripts/fetch-voice-models.sh`, `backend/tests/voice/test_voice_live.py`

**Interfaces:**
- Consumes: `SpeechToText`, `TextToSpeech` (engine reali), i modelli scaricati.

- [ ] **Step 1: Scrivere lo script di provisioning**

File `scripts/fetch-voice-models.sh`:
```bash
#!/bin/bash
# Download the voice models ONCE: the faster-whisper STT model is fetched on
# first use by the library; here we download the Piper voices. Verify EACH
# voice's license is permissive (§3) before use, and record it in STATO_TECNICO.
set -euo pipefail

VOICE_DIR="${BUSSOLA_VOICE_MODEL_DIR:-models/voice}"
BASE="https://huggingface.co/rhasspy/piper-voices/resolve/main"
mkdir -p "$VOICE_DIR"

# language : relative path on the piper-voices repo (voice .onnx + .onnx.json)
# NOTE: confirm the exact voice + license before committing to it (§3).
download() {
  local rel="$1" name="$2"
  for ext in onnx onnx.json; do
    if [ ! -f "$VOICE_DIR/$name.$ext" ]; then
      echo "Downloading $name.$ext ..."
      curl -L --fail -o "$VOICE_DIR/$name.$ext.part" "$BASE/$rel.$ext"
      mv "$VOICE_DIR/$name.$ext.part" "$VOICE_DIR/$name.$ext"
    fi
  done
}

download "it/it_IT/paola/medium/it_IT-paola-medium" "it_IT-paola-medium"
download "en/en_US/lessac/medium/en_US-lessac-medium" "en_US-lessac-medium"
download "fr/fr_FR/siwis/medium/fr_FR-siwis-medium" "fr_FR-siwis-medium"
download "es/es_ES/davefx/medium/es_ES-davefx-medium" "es_ES-davefx-medium"

echo "Done. Verify each voice's LICENSE (MODEL_CARD) is permissive before use."
```

- [ ] **Step 2: Scrivere il test d'integrazione**

File `backend/tests/voice/test_voice_live.py`:
```python
"""Round-trip TTS -> STT with the real models (requires_voice). Self-contained:
synthesize a known phrase, transcribe the audio back, assert the key words
survive. Skips unless faster-whisper + piper are installed and the models are
present. Synthetic phrases only (§9)."""

from __future__ import annotations

import pytest

from bussola.voice.stt import SpeechToText
from bussola.voice.tts import TextToSpeech


def _voice_available() -> bool:
    try:
        import faster_whisper  # noqa: F401
        import piper  # noqa: F401
    except Exception:
        return False
    from bussola.voice import config

    return bool(TextToSpeech(voices=config.PIPER_VOICES).synthesize("prova", "it"))


requires_voice = pytest.mark.skipif(
    not _voice_available(), reason="modelli voce/librerie non disponibili"
)


@requires_voice
@pytest.mark.parametrize(
    "language,phrase,keyword",
    [
        ("it", "so cucinare", "cucin"),
        ("en", "i can cook", "cook"),
    ],
)
def test_tts_stt_round_trip(language: str, phrase: str, keyword: str):
    audio = TextToSpeech().synthesize(phrase, language)
    assert audio is not None  # a configured language must produce audio
    transcription = SpeechToText().transcribe(audio, language)
    assert keyword.lower() in transcription.text.lower()
```

- [ ] **Step 3: Prerequisiti ed esecuzione (io)**

```bash
backend/.venv/bin/pip install -e "backend[voice]"    # faster-whisper + piper-tts
# system: piper può richiedere espeak-ng (es. `apt-get install espeak-ng`)
bash scripts/fetch-voice-models.sh                   # scarica le voci Piper
backend/.venv/bin/pytest backend/tests/voice/test_voice_live.py -v
```
Expected: PASS — il round-trip su it/en riconosce le parole chiave. Se un passo non regge, investigare il modello/voce (non indebolire le asserzioni); se una voce ha licenza non permissiva, sostituirla o passare quella lingua a testo.

- [ ] **Step 4: Gate completo + commit**
```bash
backend/.venv/bin/pytest backend/tests -q
backend/.venv/bin/ruff check backend/ && backend/.venv/bin/ruff format --check backend/src/bussola/voice backend/tests/voice
backend/.venv/bin/mypy --config-file backend/pyproject.toml backend/src
chmod +x scripts/fetch-voice-models.sh
git add scripts/fetch-voice-models.sh backend/tests/voice/test_voice_live.py
git commit -m "test(voice): round-trip TTS->STT coi modelli reali (requires_voice) + provisioning"
```

---

## Note di chiusura (scelte di ambito)

- **HTTP / cattura audio nel browser / kiosk / wiring nel colloquio** → S8 (frontend).
- **Arabo-TTS**: attivabile aggiungendo una voce permissiva a `PIPER_VOICES` **dopo** validazione sul pilota (§8).
- **VAD / streaming / barge-in** → Fase 2.
- **Scelta del device STT (`cuda`)**: da misurare sul pilota; default `cpu` per sicurezza VRAM (§5).

## Verifica di copertura (spec → task)

| Requisito (spec/§) | Task |
|---|---|
| Config device CPU default + no arabo TTS (§3.2/§3.6) | 1 |
| Contratto di degrado STT → VoiceUnavailable (§3.4) | 2 |
| Contratto di degrado TTS → None (§3.4) | 3 |
| Multilingua + selezione voce per lingua (§2) | 3 |
| Licenze permissive delle voci (§3.5) | 4 (provisioning + vetting) |
| Integrazione reale round-trip (§6) | 4 |
| TDD, dati sintetici, codice inglese (§9/§11) | Tutti |
